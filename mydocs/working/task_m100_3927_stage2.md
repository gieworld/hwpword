---
kind: working
status: active
canonical: mydocs/plans/task_m100_3674.md
last_verified: 2026-08-04
---

# Task #3927 Stage 2 — 행4 분해: 줄 수는 맞고 줄 높이가 크다

## 분해 결과 (RHWP_DIAG_ROWH, pi=23 r=4 c=2)

```
DIAG_ROWH r=4 c=2 decl=39.3 req=422.5 content=392.3 pad=30.2 stored=true
```

| 성분 | rhwp | 한컴(PDF p287 실측) |
|---|---|---|
| 줄 수 | 16 (저장 p0 12 + p1 4) | **16** — 일치 |
| 답변 본문 세로 | **392.3px** | **301.2px** |
| 줄당 높이 | **24.5px** | **18.8px** (1.30배 차) |
| 표 전체 | 549.8px | 389.1px |

**줄 수와 줄바꿈은 정확하다** — 폭·글꼴 계산은 정합. 오차는 **줄당 높이 단일 축**이다.

## 저장 데이터와의 관계

셀 문단 저장 LINE_SEG: `lh=1150HU(15.3px)`, `vpos` 간격 `1838HU(24.5px)`, `ls=688`.

- rhwp 는 **vpos 간격(24.5px)** 을 줄 높이로 쓴다 → content 392.3 = 16 × 24.5.
- 한컴 실제 조판은 **18.8px** — `lh`(15.3)보다 크고 vpos 간격(24.5)보다 작다.
- 셀 문단 줄간격 설정은 `line=160/Percent` 계열(호스트 PS 표본). 15.3 × 1.23 ≈ 18.8,
  24.5 = 15.3 × 1.60 — **rhwp 가 160% 를 vpos 간격에 이미 반영된 값에 다시 적용**하는
  형태로 보인다(이중 적용 가설).

## 이 관찰의 무게

- 편람 잔여 발산(#3927)의 근인이 **표 분할 문턱이 아니라 셀 내 줄 높이**로 확정.
- 같은 축이 [#3674 7보](task_m100_3674_stage2.md)의 `issue_3738` 회귀 원인(행높이
  2.2배 과대)과 동일 계열 — **#3931 의 "측정 정합 선결"이 실제로 선결임을 재확인**.
- 셀 내 줄간격은 전 문서에 걸린 공통 경로라 파급이 크다. 수정은 **10k 서베이 게이트
  필수**이며, 이 타스크 범위를 넘을 가능성이 높다.

## Stage 3 제안 (작업지시자 결정)

1. (권고) 이 타스크는 **진단 완결**로 종결 — 근인·수치·오라클을 #3931 에 이관하고,
   측정 정합 수정은 그 이슈에서 통합 처리. #3674 3안과 같은 판단.
2. 셀 줄높이 이중 적용 가설을 이 타스크에서 검증·수정 — 파급 큼, 10k 필요.
