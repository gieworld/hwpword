# task_m100_4541 stage1 — 대전 갱신: 살아있음 실증 (74명령)

- 이슈: #4541 / 브랜치: task_m100_4541 (task_m100_4539 + task_m100_4530 병합)
- 산출물: 생성기 가족 표 확장(서명·하네스 6항) + 재생성 델타(정확히 2장) +
  결정론 정직 조항(키 무작위 계열은 계약만 + 사유)

## 실증 내용

- 명령 71→74(서명 2·하네스 1 우산): **생성기 재실행 한 번**으로 검증 사다리
  장이 keygen·verify-signature·harness(하위 3 포함) 절을 얻고, 커버리지 가드
  2/2·--check 멱등 0변경 유지. "썩지 않는 교본" 주장의 전 경로 실증.
- 표본 결정론 정직: 키 생성 무작위 계열은 실측 표본 대신 계약만 라벨 + 정본
  위치(계약 테스트·선언 문서) 지시 — 살아있는 척하는 죽은 예시 금지 원칙.
