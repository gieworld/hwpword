# Task M100 #4327 Stage 1 — 사용자·생태계 수요 실조사와 로드맵 사실 정정

- 이슈: [#4327](https://github.com/edwardkim/rhwp/issues/4327)
- 기준 브랜치: `upstream/devel`
- 작업 브랜치: `task_m100_4327`
- 작성일: 2026-08-09 KST
- 상태: 문서 전용(코드·워크플로·렌더 영향 없음), 검증 완료

## 수행 내용

1. **3원 실조사 수행 (2026-08-09)** — ① 한국 커뮤니티 여론(웹검색 24회·원문 20건),
   ② 글로벌 생태계·발견 채널 감사(웹검색 23회·원문 13건), ③ GitHub 실사(api.github.com
   비인증 실측). 병렬 조사 에이전트 3기로 수집.
2. **결정 근거 직접 재검증** — 등급 변경·게이트 판정·원문 인용에 쓰인 근거 전건을
   수집과 별도로 재확인:
   - #60 메인테이너 코멘트 원문 (gh CLI): "hwp 에디터 만들기는 일종의 부산물 …
     hwp 개발 전문 에이전트로 작업이 가능하게 하는게 더 큰 목표"
   - v0.8.2 릴리스 자산·다운로드 수치 (gh CLI): linux 819 · windows 346 ·
     macOS 137+15 · SHA256SUMS 219
   - k-skill `docs/features/rhwp-advanced.md` 원문: "편집 서브커맨드는 없다.
     v0.7.3 기준 …" + 자체 CLI 우회 서술
   - treesoop/hwp-mcp README: `@rhwp/core` 기반 · 67★ · `npx` 원라인 · "0.7.7
     라운드트립 한계" 명기
   - ONLYOFFICE/DocumentServer#3659 원문 (open · 2026-04-21 · 통합 요청)
   - mete0r/pyhwp#169 원문 (표 안 텍스트 미추출 · 2020)
3. **에이전트 보고 기각 2건** — 로컬 실물 대조에서 어긋난 서술을 산출물에서 제외:
   "루트 llms.txt 404"(실물: 루트에 존재), "README 에 MCP 언급 0회"(실물: "AI
   에이전트에서 쓰기 (MCP)" 절 존재). 조사 문서 §0 에 기각 사실을 명시했다.
4. **산출물 작성**
   - 신설: `mydocs/tech/agent_roadmap/user_demand_survey_2026h2.md` — 5관문 선별
     승계, 수요 지형·발견 채널 감사·생태계 소비자·설치 실물 대조, 파이 순위
     P1~P5, 로드맵 3판정 대조, 신규 제안 2건(U1 프레임워크 로더 · U2 소비자 대사
     채널), 다음 착수 순서(게이트 사슬), 한계 명시.
   - 정정: `track_d_discovery.md` — R38 `[가설]`→`[실측]`(릴리스 파이프라인
     #612·자산·다운로드 실재), 지금·게이트 재서술, R39 실측 추가.
   - 정정: `track_g_bindings.md` — R63 수요 실측 추가(#227·서드파티 바인딩·README
     "PyPI 배포 전").
   - 정정: `track_i_standards.md` — R86 지금 정정(외부 소비자 실재)+게이트 재평가
     부기, R87 실측 추가(발신 0 이 원인인 도달 0).
   - 재생성: `agent_roadmap/README.md` 진행률 블록 — `tools/roadmap_progress.py
     --write` (완료 35 · 실측 11 · 문서 7 · 이슈 3 · 가설 44). 조사 문서 2편의
     진입점 표("조사 문서" 절)를 트랙 지도 아래에 추가.

## 판단 기록

- R38 승급 근거: `[실측]` 정의("측정·재현이 존재")에 부합 — 릴리스 파이프라인이
  실재하고 자산·다운로드가 측정된다. DoD(채널 1줄 설치)는 미달이므로 `[완료]` 는
  아니다.
- R86·R63·R39·R87 은 등급 유지 — 해당 단계 자체의 실물(사례집·PyPI·등재·이중화)은
  여전히 없다. 갱신은 "지금" 서술의 사실 정정과 게이트 근거 주입까지다.
- 채널 심사 요건(R38 게이트 ②)은 조사 범위 밖 — 미충족으로 정직하게 남겼다.
- 신규 제안은 캡 안에서 2건으로 멈췄다 — U1(프레임워크 로더)은 로드맵 전 트랙
  대조에서 공백 확인, U2 는 신규 번호가 아니라 R67×R83 DoD 확장으로 제안(드리프트
  표면을 만들지 않기 위해).

## 검증

- `python tools/roadmap_progress.py` (검증 모드) — 전부 통과(결번 0 · 중복 0 ·
  등급 어휘 일치 · README 집계 일치).
- 문서 메타데이터 검사 · Markdown 링크 검사 · `git diff --check` — PR 전 실행,
  결과는 PR 본문에 기재.
- Cargo·WASM·시각 검증 — 해당 없음(문서 전용).

## 다음 (별도 이슈)

1. R67×R83 공동 설계·구현 — 이 조사로 착수 게이트 충족. U2 를 DoD 에 편입.
2. R63 휠 파이프라인 → R38 잔여(README 도달 1줄 · 채널 등재) → R39 등재 → U1.
