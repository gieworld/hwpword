# 수행계획 — task_m100_4159

- **이슈**: [#4159](https://github.com/edwardkim/rhwp/issues/4159)
- **브랜치**: `task_m100_4159_nested_table_bottom_clip`
- **기준**: `upstream/devel` `06f8ebcca`
- **작성 시각**: 2026-08-07 KST

## 1. 목표

`samples/basic/issue2007_nested_cell_pagination_42065.hwp` 물리 3쪽에서 종료 재귀 중첩 표의
bottom 선이 조상 분할 `TableCell` clip 아래로 약 1.89px 내려가 사라지는 결함을 고친다.

## 2. 변경 경계

- 테두리 생성·backend paint가 아니라 `layout_partial_table`의 셀 bbox/clip 포섭 계약을 고친다.
- 셀의 마지막 유닛까지 소비한 종료 조각에만 자손 하단 포섭을 허용한다.
- 비종료 조각의 clip을 넓혀 다음 쪽 소속 콘텐츠나 terminal border가 미리 보이게 하지 않는다.
- 기존 페이지네이션 cursor, 전체 17쪽, 물리 2·3쪽 텍스트, 10·11쪽 저장 프레임,
  15·16쪽 자식 표 이음 계약을 보존한다.

## 3. 구현·래칫 순서

1. 실제 fixture 물리 3쪽의 종료 nested bottom `Line`과 모든 `clip=true` 조상 `TableCell`을
   찾는 구조 회귀를 먼저 추가해 red를 확인한다.
2. 분할 셀의 가시 유닛 창이 terminal인지 판정하고, 재귀 표 자손의 stroke 하단만큼 필요한
   셀 bbox 확장을 적용한다.
3. 비종료 synthetic/fixture 조각이 기존 clip 하단을 유지하는 단위 또는 구조 회귀를 둔다.
4. 실제 SVG에서 bottom 선 stroke가 조상 clip 안에 포함되는지 확인한다.
5. 새 WASM의 Canvas2D 물리 3쪽에서 종료 수평선 픽셀을 검증하고 `output/4159/`에 증적을 남긴다.

## 4. 검증 게이트

- #4159 focused Rust 구조/SVG 테스트
- `tests/issue_2007_nested_cell_pagination.rs` 기존 4개
- fmt, diff, focused Clippy
- release WASM build와 #4159 Canvas2D E2E
- #4069 인접 페이지 계약 및 시각 대조
- 집중 결과 보고 후 작업지시자 별도 승인 시 `local_validation.md` 4.3 전체 PR 게이트

원격 push, PR 생성, 이슈 comment·close는 별도 승인 전 수행하지 않는다.
