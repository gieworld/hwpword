---
kind: guide
status: active
canonical: mydocs/manual/agent_codex/85_진단_프로브.md
last_verified: 2026-08-11
generated: tools/gen_agent_codex.py — 수기 수정 금지, 재생성으로 갱신
---

# 진단·프로브 — 개발자 표면 (에이전트 통상 작업 비권장)

> 렌더·파서 개발용 저수준 진단이다. 문서 작업 에이전트는 이 장을
> 쓸 일이 거의 없다 — 레이아웃 버그 조사 시에만 rhwp-cli 스킬의
> 디버깅 순서를 따라 진입하라.

### `bench` — 성능 벤치마크

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `bench <파일...> | --batch <폴더> [-n <반복수>] [--tsv <출력.tsv>]`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `core-pages` — 코어 페이지 수 프로브

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `diag` — 문서 구조 진단(번호/글머리표/개요)

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `diag <파일.hwp>`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `dump` — 문서 조판부호 구조 덤프

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `dump <파일.hwp|파일.hwpx|파일.hml> [--section <번호>] [--para <번호>]`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `dump-endnote-lines` — 미주 줄 배치 덤프

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `dump-endnote-lines <파일.hwp> <section> <para> <control> [note-para]`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `dump-extents` — 레이아웃 트리 항목별 실제 extent 덤프 (쪽 밖 배치 조사용)

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `dump-note-shape` — 각주/미주 모양 덤프

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `dump-note-shape <파일.hwp|파일.hwpx>`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `dump-records` — 저수준 레코드 스트림 덤프

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `dump-records <파일.hwp>`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `export-png` — 문서를 페이지별 PNG로 렌더 (native-skia)

- 종류: `export` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `export-png <파일.hwp> [옵션]   (native-skia feature 필요)`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `export-render-tree` — 페이지별 render tree bbox JSON 덤프

- 종류: `export` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `export-render-tree <파일.hwp> [옵션]`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `gen-pua` — PUA 샘플 생성

- 종류: `internal` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `gen-pua                             PUA 문자 테스트 HWP 생성`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `gen-table` — 표 샘플 생성

- 종류: `internal` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `gen-table                           표 테스트 HWP 생성`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-anchor-trace` — 앵커 추적

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-anchor-trace <파일.hwp> --needle <텍스트> [--section N] [--window N] [--out <path>]`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-borderfill-diagonal-probe` — 테두리 대각선 프로브

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-borderfill-diagonal-probe <oracle.hwp> <generated.hwp> --out-dir <폴더>`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-cell-header-probe` — 셀 헤더 프로브

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-cell-header-probe <oracle.hwp> <generated.hwp> --out-dir <폴더>`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-char-shape-audit` — CHAR_SHAPE provenance audit

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-char-shape-audit <hancom-oracle.hwp> <generated.hwp> --out <보고서.md> [--source-hwpx <원본.hwpx>]`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-contract-analyze` — HWPX→HWP5 저장 계약 분석

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-contract-analyze <source.hwpx> <oracle.hwp> <generated.hwp> --out-dir <폴더>`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-contract-probe` — HWP5 저장 계약 프로브

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-contract-probe <oracle.hwp> <generated.hwp> --out-dir <폴더>`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-ctrl-data-trace` — CTRL_DATA 추적

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-ctrl-data-trace <oracle.hwp> <generated.hwp> --out <path> [--section N] [--record-index N]`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-first-para-control-probe` — 첫 문단 컨트롤 프로브

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-first-para-control-probe <oracle.hwp> <generated.hwp> --out-dir <폴더>`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-inventory` — HWP5 레코드 인벤토리

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-inventory <파일.hwp> [--format jsonl|md] [--section N] [--out <path>]`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-inventory-diff` — HWP5 인벤토리 비교

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-inventory-diff <oracle.hwp> <generated.hwp> [--align index|lcs] [--report diff|hints|bundles|table-fields|table-probe-plan] [--focus all|table|shape|ctrl|missing|docinfo] [--window N] [--format jsonl|md] [--section N] [--out <path>]`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-mel-personnel-probe` — 특정 샘플 재현 프로브

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-mel-personnel-probe <oracle.hwp> <generated.hwp> --out-dir <폴더>`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-roundtrip` — HWP5 왕복 무손실 게이트

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-roundtrip <파일.hwp | --batch 폴더> [-o <출력폴더>]`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `hwp5-table-probe` — 표 저장 프로브

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `hwp5-table-probe <oracle.hwp> <generated.hwp> --out-dir <폴더>`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `measure-width` — 텍스트 폭 측정 프로브

- 종류: `diagnostic` · exit 규약: 0 성공 / 1 IO / 2 사용법

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `test-caption` — 캡션 테스트

- 종류: `internal` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `test-caption <파일.hwp> [-o <폴더>] 캡션 라운드트립 검증`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `test-field` — 누름틀 왕복 테스트

- 종류: `internal` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `test-field <파일.hwp>               필드 라운드트립 검증`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.

### `test-shape` — 도형 왕복 테스트

- 종류: `internal` · exit 규약: 0 성공 / 1 IO / 2 사용법
- 사용법: `test-shape <입력.hwp> <출력.hwp>    도형 라운드트립 검증`

> **계약만** — 입력 합성 비용 또는 산출 부피 때문에 표본 실행을 싣지 않는다 — 계약(플래그·봉투 필드·출처)은 아래가 전부이며 자기서술에서 생성됐다.
