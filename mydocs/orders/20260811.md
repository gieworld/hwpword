# 오늘 할일 - 2026년 8월 11일

## 공통 — 운영 작업

| Issue | 타스크 | 상태 | 비고 |
| --- | --- | --- | --- |
| [#3790](https://github.com/edwardkim/rhwp/issues/3790) | Stage 5B CodeQL 언어별 선택 실행 | 진행중 | Draft PR #4519 최신 devel 반영·reviewer `edwardkim` 지정·검토 기록 완료; 최종 full CI·CodeQL 확인 대기 |
| [#4029](https://github.com/edwardkim/rhwp/issues/4029) | cold-cache release test archive 완주 정책 | 진행중 | Draft PR #4581의 일반 `release-test/30`·cold `release/60` canary 통과; 최신 `devel` 동기화 후 CI 재검증 |
| [#4586](https://github.com/edwardkim/rhwp/issues/4586) | gym T12 HWPX 형식·판정 계약 보정 | PR #4598 self-review | T12 focused 2/2, release-test 5,763/5,763와 initial code candidate Full CI 통과; review-only 기록 push 준비 |
| [#4601](https://github.com/edwardkim/rhwp/issues/4601) | v0.8.3 릴리즈 | Stage 3 완료 | `upstream/devel@572786d02` 기준; 버전·CHANGELOG·라이선스·스토어·에이전트 계약 갱신 |

## Issue #4601 - v0.8.3 릴리즈

- 직전 릴리즈는 `v0.8.2@9b16aa9e2`, 이번 릴리즈 준비 기준선은
  `upstream/devel@572786d02`로 재확정했다. 준비 재개 시점의 열린 PR 10건은 포함하지 않는다.
- `main@2dced7bfe`에는 기본 브랜치에서만 등록되는 workflow를 조기 반영한 독자 계보가 남아 있다.
  `task/4601-release-v0.8.3`에서 `main`을 먼저 병합하고, 오래된 workflow 충돌은 현재 `devel`
  내용을 유지한 뒤 병합 전후 트리 동일성을 확인한다.
- 버전·CHANGELOG·스토어 제출 문서를 `0.8.3`으로 갱신하고, v0.8.2 이후 추가된 Node/Python
  바인딩·바이너리·deb/rpm/MSI·Docker 배포 표면도 릴리즈 게이트에 포함한다.
- 작업 브랜치의 준비 변경은 `devel` 대상 PR과 CI로 통합한다. 그 뒤 `devel`을 `main` 대상
  release PR로 merge commit 방식으로 반영하고, 태그·GitHub Release·각 배포 채널을 순차 확인한다.
- Stage 1 merge commit `1ae2c393c`가 `main@2dced7bfe`를 두 번째 부모로 흡수했다. workflow
  충돌 3건은 최신 devel로 해소했고, 병합 전후 tree hash는
  `c03667d17444fc2b67eb1bf4422ed954df908a1d`로 동일하다.
- [Stage 2 범위 조사](../working/task_m100_4601_stage2.md)에서 `v0.8.2` 이후 병합 PR 190건,
  커밋 2,095개, 변경 파일 4,186개를 분류했다. 암호 문서·중첩 표·편집·MCP/에이전트 계약과
  새 패키지/설치 채널을 CHANGELOG 및 검증의 필수 범위로 확정했다.
- [Stage 3 릴리즈 후보](../working/task_m100_4601_stage3.md)는 제품 version·lockfile,
  한국어/영어 CHANGELOG, README·로드맵, 라이선스 243개 외부 package 인벤토리와 확장
  스토어 문서를 `0.8.3`으로 맞췄다. 새 release 바이너리는 CLI 83개·MCP 82개와
  IR 1.0 / capabilities 1.3 / plan 1.1을 자기서술했고 Node 생성 타입도 정렬됐다.

## PR #4609 - stale run 취소 완료 race 보정

- [PR #4609](https://github.com/edwardkim/rhwp/pull/4609)은 [#4608](https://github.com/edwardkim/rhwp/issues/4608)을
  해결한다. GitHub `force-cancel`이 500을 반환해도 대상 run이 실제 `completed`이면 정상 경과로 처리하고,
  active 대상 또는 재조회 실패는 계속 오류로 남긴다.
- code candidate `81603f81d`에서 stale run workflow contract 4건, CI impact workflow contract 27건과
  `git diff --check`를 통과했다. Rust·renderer·WASM 구현은 변경하지 않아 전체 Cargo 회귀와 시각 검증은
  범위 밖이다.
- [검토 기록](../pr/archives/pr_4609_review.md)을 같은 PR diff에 포함한다. 최신 head의 Full CI와 CodeQL이
  통과하면 작업지시자 승인 뒤 merge하고 issue close 상태와 branch 정리를 수행한다.

## PR #4602 - Subsecond 핫패치 4개 원 PR 통합

- [PR #4602](https://github.com/edwardkim/rhwp/pull/4602)는 [#4576](https://github.com/edwardkim/rhwp/issues/4576),
  [#4577](https://github.com/edwardkim/rhwp/issues/4577), [#4578](https://github.com/edwardkim/rhwp/issues/4578),
  [#4579](https://github.com/edwardkim/rhwp/issues/4579)를 해결한다. 원 PR #4584, #4590, #4594, #4597은
  `#4584 -> #4590 -> #4594 -> #4597` 순으로 누적했다.
- 최초 기준선에서 release-test nextest 5,764건, Native Skia 3종, WASM build, Studio TypeScript와
  Studio test 847건을 통과했다. 최신 `upstream/devel` `4f9e4ae6` rebase는 충돌 없이 끝났고,
  기준선 전진 뒤 중복 nextest는 작업지시자 지시에 따라 중단했다. 이 종료 코드 `130`은 통과로 기록하지
  않는다.
- [통합 검토 기록](../pr/archives/pr_4602_review.md), [통합 실행 기록](../pr/archives/pr_4602_review_impl.md),
  원 PR별 archive review와 누적 실행 기록을 PR diff에 포함한다. 최신 head의 CI·CodeQL·필요한 Render Diff가
  통과하면 작업지시자 승인 뒤 merge하고 원 PR close/comment와 branch 정리를 수행한다.

## PR #4598 - gym T12 HWPX 형식·판정 계약 보정

- [PR #4598](https://github.com/edwardkim/rhwp/pull/4598)을 `devel` 대상 Open PR로 생성했다. assignee
  `edwardkim`, milestone `v1.0.0`, 변경 범위 기반 labels를 적용했다.
- 작성자와 self-review 담당이 같은 `edwardkim`이므로 자기 reviewer request와 `APPROVE`는 시도하지
  않는다. 검토 결과는 최신 head에 `COMMENTED` review로 게시한다.
- initial code candidate `4f24c73e9`는 로컬 focused·전체 release-test·fmt·clippy·문서 링크 검사와
  최신 `upstream/devel` merge simulation을 통과했다. GitHub Full CI·CodeQL 3개 언어·Render Diff도
  모두 성공했으므로 [검토 기록](../pr/archives/pr_4598_review.md)을 trailing review-only commit으로 push한다.
- 최신 review-only head의 fast-pass·required checks·mergeability와 self-review 게시를 확인한 뒤 별도
  작업지시자 승인을 받아 merge한다.

## PR #4587 - CodeQL neutral 요약의 review-only 재사용

- [PR #4587](https://github.com/edwardkim/rhwp/pull/4587)은 [#4585](https://github.com/edwardkim/rhwp/issues/4585)를
  해결한다. review-only CodeQL preflight가 GHAS 집계 `CodeQL` check의 `neutral`을 개별 Analyze 성공과
  함께 허용하도록 보정하되, `skipped`·누락·대기·실패·취소는 계속 full 분석으로 fail-closed 한다.
- code candidate `725568649`에서 Full CI, CodeQL의 세 language Analyze, Native Skia, lint, 모든
  default-feature shard가 성공했다. trailing docs-only commit에서 CodeQL preflight가 이 candidate를
  재사용하고 Analyze job을 skip하는지 확인한다.
- [검토 기록](../pr/archives/pr_4587_review.md)에 #4525 재현 근거, local workflow contract 23건,
  GitHub run 결과를 남겼다. 외부 PR 통합 검토는 기본적으로 사용자가 열어 둔 작업공간의 최신 `devel`
  위 가시성 branch에서 cherry-pick하도록 공식 절차도 보강했다.

## PR #4525 - stableIndex 문서 경로 정렬 계약

- [PR #4525](https://github.com/edwardkim/rhwp/pull/4525)는 `stableIndex` 정렬 계약을 숫자에서 문서 경로
  배열로 바꾸고 `node.id` 의존을 제거한다. 메인터너는 Studio hit-test E2E를 배열 계약에 맞추고,
  최신 기준선 호환을 위해 `TableNode` 테스트 literal의 `cell_context: None` 초기화 한 곳을 보완했다.
- 원 source commit은 재작성하지 않았다. 최신 `upstream/devel`을 source에 merge한 뒤 호환 보정 commit을
  분리했고, 최종 code candidate `6edb700d1`에서 CI, CodeQL, Render Diff와 로컬 focused Rust test,
  workspace build, WASM Studio E2E를 모두 통과했다.
- [검토 기록](../pr/archives/pr_4525_review.md)과 [이행 기록](../pr/archives/pr_4525_review_impl.md)에
  기준선 호환 실패와 보정 근거를 남겼다. 공식 PR workflow에는 `MERGEABLE`만으로 current-base 호환을
  단정하지 않는 외부 PR 보정 절차를 추가했다.
- 이 trailing docs-only commit의 fast-pass와 최신 aggregate가 성공하면 작업지시자 승인에 따라 merge하고,
  [#4334](https://github.com/edwardkim/rhwp/issues/4334)의 자동 close 상태, contributor comment,
  `devel` sync와 review worktree 정리를 수행한다.

## PR #4574 - kevin9327 검증 사다리 18건 통합

- [PR #4574](https://github.com/edwardkim/rhwp/pull/4574)는 #4465, #4510, #4511, #4529, #4534, #4536,
  #4538, #4540, #4542, #4544, #4546, #4548, #4550, #4552, #4557, #4559, #4562, #4563을 오래된
  PR 번호순으로 누적한다. 원 source의 `devel` merge commit은 제외했고, 최신 `upstream/devel` 위로 rebase했다.
- 메인터너 보정은 생성 코덱스가 실제 top-level 명령만 기록하도록 정합화하고, 온보딩 지침과 기존
  `llms.txt` roadmap 행을 함께 보존한다. gym 제출 산출물은 contributor cleanup 범위로 제외된 상태를 유지한다.
- 로컬에서 `cargo fmt --all -- --check`, focused nextest 37건, release-test nextest 전체
  5,757 passed / 36 skipped / 7 slow, 생성 코덱스 83개 명령·변경 0을 완료했다. rebase 기준선 전진분은
  CI workflow·Python 도구·문서뿐이고 Rust/Node tree가 동일하므로 전체 회귀를 반복하지 않았다.
- 첫 CI의 생성 타입 최신 검사는 Node 봉투 47개가 capabilities 52개를 따라가지 못해 실패했다. 누락된
  audit-report, conformance, disclose, recall-scope, settle 타입을 재생성했고 `npm run gen:check`를 통과했다.
  보정한 최신 PR head의 CI를 다시 확인한다.
- [통합 검토 기록](../pr/archives/pr_4574_review.md)과 원 PR별 archive review를 이 PR diff에 포함했다.
  작성자와 동일한 계정에는 GitHub가 self-review 요청을 허용하지 않아 reviewer는 비어 있다. 최신 head의
  GitHub Actions가 통과하면 작업지시자 승인 뒤 merge하고, 이후 원 PR close/comment, `devel` sync와
  local branch·worktree 정리를 수행한다.

## PR #4571 - HWP3 음영과 HWPX 차트 변환 정합

- [PR #4571](https://github.com/edwardkim/rhwp/pull/4571)은 [#4366](https://github.com/edwardkim/rhwp/pull/4366)과
  [#4499](https://github.com/edwardkim/rhwp/pull/4499)을 최신 `devel`에 누적하고, HWPX의 실제 검정
  음영을 `none`으로 소거하던 라이터 결함을 메인터너 보정으로 함께 처리한다.
- 로컬에서 `cargo fmt --check`, `cargo clippy --profile release-test --all-targets -- -D warnings`,
  HWP3 음영 계약 7건, 검정 음영 저장 단위 테스트 1건, release-test nextest 전체 5,730건을
  통과했다. 차트 표본은 한컴 2020 PDF의 1쪽 144 DPI 래스터 SHA-256이 원본과 같다.
- 원 PR 검토·구현 기록과 PNG 증적은 [PR #4571 검토](../pr/archives/pr_4571_review.md) 및
  archive 문서에 같은 PR diff로 포함했다. 별도 문서 전용 PR은 만들지 않는다.
- 최신 PR head의 GitHub Actions와 branch protection의 approval 요구사항을 확인한 뒤 merge한다.
  merge 뒤 #4155/#4099 상태, 원 #4366/#4499 close/comment, `devel` sync와 review branch/worktree
  정리를 수행한다. 자동 번호·머리말·페이지 나눔 fidelity는 [#3820](https://github.com/edwardkim/rhwp/issues/3820)에서 계속 추적한다.

## PR #4517 - 76076 표 분할과 중첩 셀 편집 보정

- [PR #4517](https://github.com/edwardkim/rhwp/pull/4517)은 2026-08-10에 merge commit
  `04520749b51b8ce8a10ccfb7b33516a87c3ce041`으로 `devel`에 반영됐다.
- source head `cd25076700eef8f9b9cccda05d8b0675c84eff4e`의 GitHub CI에서 Build & Test,
  CodeQL, Render Diff, Native Skia, Lint와 frontend package gate가 성공했다. 로컬에서는
  WASM build, release-test nextest 전체, `npm test` 835 pass / 0 fail / 1 skipped을 확인했다.
- 원 PR에 없던 [검토 기록](../pr/archives/pr_4517_review.md)과 이 오늘할일은 문서 전용
  review-only fast-pass PR로 보완한다.
- [#3820](https://github.com/edwardkim/rhwp/issues/3820)은 열린 상태로 유지한다. 17쪽 HWPX의
  5쪽 개체 주변 본문 흐름이 기준 PDF와 다른 잔여 시각 충실도 결함을 같은 이슈에서 계속 추적한다.
