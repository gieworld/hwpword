# 오늘 할일 - 2026년 8월 10일

## PR #4513 - humdrum00001010 CI 성공 PR 11건 누적 통합

- [PR #4513](https://github.com/edwardkim/rhwp/pull/4513)는 `upstream/devel`
  `c20377b9e` 위의 `pr/devel-humdrum00001010-batch`에서 Open PR로 생성됐다.
- #4443, #4446, #4454, #4462, #4469, #4497, #4500, #4501, #4502, #4503, #4504의
  기능·필요 작업 기록 commit을 provenance 보존 형태로 누적했다. Draft·의도적 red #4315는 제외했다.
- #4462의 공용 caption parser 이름 변경과 #4503의 신규 호출이 만나는 누적 컴파일 결함은
  메인터너 보정 `2d2f42524`로 호출 한 곳만 정합화했다.
- code candidate `4b7b81728`은 release-test 5,645건, Studio 관련 test 23건, content-loss 저장 E2E
  345건, GitHub Full CI·Build & Test·CodeQL·Canvas visual diff·Native Skia를 통과했다.
- [PR #4513 review](../pr/archives/pr_4513_review.md)와
  [이행 기록](../pr/archives/pr_4513_review_impl.md)을 trailing review-only commit으로 반영한 뒤,
  최신 head fast-pass·mergeability·작업지시자 merge 승인을 확인한다.

## PR #4274 - 웹한글컨트롤 호환 층과 Windows Oracle 이식성 보정

- [PR #4274](https://github.com/edwardkim/rhwp/pull/4274)는 `devel`에 merge commit
  `92100edbf6bb1dade47af1342a49b089a6ed2e1c`으로 병합됐다.
- 최신 head의 GitHub Full CI, CodeQL, Canvas visual diff, Native Skia와 Build & Test가
  모두 성공했다. Windows Hancom 2022 Oracle에서도 82개 시나리오 3,519/3,519 호출이 일치했다.
- 메인터너 보정은 Windows 절대경로 제거, package gate의 종료 대기, 저장 질문 차단,
  code page 독립 Oracle 계약을 포함한다.
- [구현·검증 기록](../pr/archives/pr_4274_review_impl.md)을 archive로 보존했다.
- 그림 삽입 좌표 결함을 다룬 [issue #4347](https://github.com/edwardkim/rhwp/issues/4347)은
  병합 결과 확인 뒤 close한다. 표 분할 전제를 반증한 issue #4359는 이미 closed 상태다.

## Issue #4467 - 프로젝트 로드맵 canonical 분리

- README의 초기 `0.5 → 1.0 → 2.0 → 3.0` 제품 로드맵을 별도 문서에서 증거 기반으로 관리한다.
- 중복 이슈 검색 후 [#4467](https://github.com/edwardkim/rhwp/issues/4467)을 등록하고 assignee를
  `edwardkim`, milestone을 `v1.0.0`, label을 `documentation`으로 지정했다.
- 최신 `upstream/devel` `629cd33db`에서 `task/4467-project-roadmap` 브랜치를 생성했다.
- [수행계획](../plans/task_m100_4467.md)을 작업지시자가 승인해 `ROADMAP.md`와 README·문서 지도
  수정을 완료했다.
- 초기 버전 축을 현재 위치·버전별 목표·완료 기준으로 상세화하고, #3907 R1~R100은
  `AI 활용과 자동화` 분야의 세부 기술 지도로 배치했다.
- Stage 1 판정 뒤 한국어 모국어 사용자인 일반 독자를 기준으로 논리 전개와 어휘를 다시 보정했다.
  [피드백](../feedback/task_m100_4467_roadmap_audience.md)과
  [Stage 2 결과](../working/task_m100_4467_stage2.md)에 판단과 변경 어휘를 기록했다.
- v1.0 이전에 이미 외부 기여자 40명 이상과 콜레보레이터 두 명이 참여한 현재 상태를 반영해,
  조판과 협업이 순차 단계가 아니라 함께 진행되는 구조로 보정했다.
  [피드백](../feedback/task_m100_4467_roadmap_parallel_progress.md)과
  [Stage 3 결과](../working/task_m100_4467_stage3.md)에 판단과 현재 근거를 기록했다.
- 공식 업스트림 배포 대상을 Chrome·Edge·Firefox·VS Code 확장과 npm으로 명시하고, CLI·MCP는
  다양한 백엔드가 재사용하는 공통 연계 표면으로 구분했다. 데스크톱·사내 뷰어·Google Docs·
  macOS·iOS·Android 앱과 서비스별 구현은 다운스트림을 기본으로 하며, 범용 엔진 수정만 업스트림
  이슈·PR로 분리하는 기준을 추가했다. [피드백](../feedback/task_m100_4467_upstream_downstream_boundary.md)과
  [Stage 4 결과](../working/task_m100_4467_stage4.md)에 경계와 PR #4322 선례를 기록했다.
- Stage 2 변경분 링크 검사(532개 문서), 메타데이터 검사(521개 문서), `git diff --check`를 통과했다.
- Stage 3 보정 뒤 현재 로컬 기준선에서 링크 검사(534개 문서, 변경 파일 14개), 메타데이터 검사
  (521개 문서), 순차 단계 오인 표현 검색과 `git diff --check`를 통과했다. 최신 원격 `devel`에
  다시 정렬한 뒤 최종 검사를 반복한다.
- Stage 4 경계 추가 뒤 링크 검사(536개 문서, 변경 파일 17개), 메타데이터 검사(521개 문서),
  공식 대상·CLI·MCP·다운스트림 제품군 표현 검사와 `git diff --check`를 통과했다.
- 작업 커밋 네 개를 최신 `upstream/devel` `c20377b9e` 위로 재배치했다. 오늘할일 문서 충돌은
  원격 기록과 #4467 기록을 모두 보존해 해결했다.
- 최신 기준 링크 검사(556개 문서, 변경 파일 18개), 메타데이터 검사(540개 문서), 공식 대상·
  다운스트림 표현 검사와 `git diff --check`를 통과했다. [최종 보고서](../report/task_m100_4467_report.md)에
  결정과 검증 결과를 정리했다.
- 작업지시자 승인 뒤 [PR #4512](https://github.com/edwardkim/rhwp/pull/4512)를 `devel` 대상 Open PR로
  생성했고, assignee `edwardkim`, milestone `v1.0.0`, label `documentation`의 1차 트리야지를 확인했다.
- 2026-08-11 작업지시자는 maintainer self-review를 승인했다. 작성자 본인 reviewer request와
  `APPROVE`는 시도하지 않고 최신 head에 `COMMENTED` review를 게시한다.
- self-review에서 후보 작성 뒤 `devel`에 합쳐진 CLI 릴리스·설치 패키지, GHCR 이미지와 GitHub Action이
  업스트림 배포 범위에서 빠진 현행화 결함을 찾았다. 제품 앱 설치 프로그램과 공용 CLI 배포물을 구분해
  로드맵과 최종 보고서를 보정하고 [PR #4512 review](../pr/archives/pr_4512_review.md)에 판정을 기록한다.
- 보정·review 기록 후보의 링크 검사(557개 문서, 변경 파일 19개), 메타데이터 검사(540개 문서),
  배포 경계 표현 검색과 `git diff --check`를 통과했다.
- 보정·review 기록 head의 최신 CI와 mergeability를 확인한 뒤 `COMMENTED` self-review를 게시한다.
  merge는 별도 작업지시자 승인 게이트로 유지한다.

## PR #4445 - humdrum00001010 기여 20건 누적 통합

- [PR #4445](https://github.com/edwardkim/rhwp/pull/4445)를 최신
  `upstream/devel` `e48fe86947fbf9a44b1b98c7037150751af541ab` 기준의
  `review/humdrum00001010-20260810` branch에서 Open PR로 생성했다.
- 원 PR 20건의 commit과 author를 보존해 누적했고, 의도적인 red-test Draft #4315는
  제외했다. 네 건의 교차 PR 충돌은 양쪽 기능을 보존했으며 표 HTML 변환 계약은
  메인테이너 보정 `1f5c4f3e4`로 고정했다.
- focused 회귀, release-test 5,567/5,567, Native Skia 58+2+4, fmt·clippy,
  표준 Docker WASM과 작업지시자 시각 판정을 통과했다.
- code·검증 기록 head `caceebd36`의 GitHub CI, CodeQL, Canvas visual diff,
  Native Skia와 Build & Test가 모두 성공했다.
- 최초 reviewer `jangster77` 지정 뒤 작업지시자가 maintainer self-review로 전환했다.
  작성자 본인 request·`APPROVE` 대신 최신 head에 `COMMENTED` review를 게시한다.
  assignee `edwardkim`, milestone `v1.0.0`, 변경 범위 기반 기존 label의 1차
  트리야지는 유지한다.
- [PR #4445 review](../pr/archives/pr_4445_review.md)와
  [통합 구현 기록](../pr/archives/pr_4445_review_impl.md)을 같은 source branch의
  trailing commit으로 반영한 뒤 최신 head CI·mergeability·`COMMENTED` self-review와
  작업지시자 merge 승인을 확인한다.

## 열린 PR 1차 트리야지

- 2026년 8월 10일 현재 열린 PR 19건을 조회했다.
- 이미 1차 트리야지가 완료된 #4418, #4366, #4341, #4315의 메타데이터는 보존했다.
- 나머지 15건(#4504, #4503, #4502, #4501, #4500, #4499, #4497, #4483, #4470,
  #4469, #4465, #4462, #4454, #4446, #4443)은 작성자를 assignee로 지정하고 milestone을
  `v1.0.0`으로 설정했으며, 제목·본문·변경 파일을 근거로 기존 label을 추가했다.
- 적용 후 열린 PR 전체를 다시 조회해 19건 모두 작성자 assignee, `v1.0.0` milestone,
  하나 이상의 내용 기반 label을 갖춘 것을 확인했다.
- PR comment, review, branch 갱신, merge, close는 수행하지 않았다.
- `gh pr edit`은 Projects Classic GraphQL 오류를 반환해, 로컬 인증된 `gh api`의 Issues REST
  endpoint로 같은 범위의 metadata만 반영했다.
