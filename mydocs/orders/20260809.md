# 오늘 할일 - 2026년 8월 9일

## 공통 — 운영 작업

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #4040 | PR #4170 merge 후 검증·종료 | 완료 | devel CI·CodeQL 성공, Native 379초, 완료: 09:47 |
| #4132 | 함수 게이트 native-skia test 2건 회복 | 완료 | PR #4310 merge, #4170 파일 게이트 규약과 Native Skia 대상 회복, 완료: 14:59 |
| #3790 | Stage 5A CodeQL 안전 재개·focused 검증 | 진행중 | Ready PR #4341 self-review 보정·devel 동기화, 보정 head full CI 대기 |

## PR #4314 - kevin9327 보안·바인딩·MCP 개선 20건 통합

- [PR #4314](https://github.com/edwardkim/rhwp/pull/4314)를 최신 `upstream/devel`
  `f62f7503f` 위 `review/kevin9327-20260809` branch로 생성했다. #4282, #4283, #4284,
  #4286, #4289, #4293-#4302, #4304-#4308의 열린 `devel` 대상 PR 20건을 번호순으로
  누적 적용했고, contributor 원 commit은 rewrite하지 않았다.
- code candidate `9a836c8e2`의 로컬 Rust nextest 5,499/5,499, fmt·clippy, Python
  251 passed·mypy·ruff, Node 427 passed·typecheck·build, 문서 metadata·link·roadmap과
  diff check가 모두 통과했다.
- 초기 Full CI 40개 check도 모두 success 또는 의도적 skip으로 완료했고, PR은
  `MERGEABLE/CLEAN`이다. 표 `u16` 사전 검사, MCP stats key 상한, Python 예외 호환성,
  Node timeout 실행 경계, OWPML source 근거는 contributor 의도를 보존한 메인터너 보정으로
  분리했다.
- PR 작성자 self reviewer request는 GitHub HTTP 422 플랫폼 제한으로 거부됐다. 최신
  review-only head의 fast-pass를 확인한 뒤 self-review `COMMENT`로 검토 기록을 남기고
  병합한다. 상세 판단은 [PR #4314 검토](../pr/archives/pr_4314_review.md)와
  [누적 구현·검토 계획](../pr/archives/pr_4282_review_impl.md)에 보존한다.

## kevin9327 PR #4282-#4308 - 누적 보안·binding·MCP·문서 검토

- 열린 `devel` 대상 PR 20건을 최신 `upstream/devel` 위의
  `review/kevin9327-20260809`에 번호순으로 누적 cherry-pick했다. #4307 Python test 충돌과
  roadmap README 충돌은 선행·후행 변경을 함께 보존하고 generator를 재실행해 해소했다.
- 표 `u16` mutation 사전 검사(#4282), 유한 MCP stats key(#4302), Python
  `TimeoutError` compatibility alias와 API 문서(#4304), Node Session timeout 뒤 child 종료
  (#4308), OWPML source 근거 경로(#4293)를 메인터너 보정 commit으로 분리했다.
- 통합 candidate `5f3e9eaf0`에서 Rust nextest 5,499/5,499, fmt, clippy, Python 251 passed와
  mypy/ruff, Node 427 passed와 typecheck/build, diff check가 모두 통과했다.
- 각 원 PR reviewer로 `jangster77`을 지정했다. 원격 head·required checks와 mergeability는
  실제 source branch 보정 push 또는 merge 직전에 PR별로 다시 확인한다. 상세 판단은
  [누적 검토 계획](../pr/archives/pr_4282_review_impl.md)과 PR별 archive review에 남긴다.

## PR #4266 - IME 조합 오버레이 재정박 및 고정 review target 검증

- contributor code head `f2a95a669`의 `compositionAnchorRect` 제거와 deferred replacement 이후 HF/FN
  char offset 재정박을 최신 `upstream/devel` `c391dbbdc`에 merge simulation했다. 충돌 없이
  `git diff --check`가 통과했고, TypeScript와 Studio 전체 test 814건도 통과했다.
- source code head의 CI preflight, Frontend package gates, CodeQL, Canvas visual diff와 Build & Test
  aggregate는 성공했다. studio 전용 변경이므로 Rust-heavy job은 의도적으로 skipped였다.
- 로컬 review Cargo cache는 host별 `target/pr-review`로 고정한다. Linux 16 vCPU/15 GiB에서 전체
  nextest 5,470/5,470이 7분 56초에 통과했고, Windows 4 vCPU/8 GiB에서도 동일 target을 재사용한
  long-baseline warm 실행이 6분 11초에 통과했다. `overflow_cell_baseline`은 nextest priority로 같은
  run 시작 시점에 배치하며, 별도 Cargo 실행을 병렬로 열지 않는다.
- 메인터너 config·검토 기록을 contributor source branch에 trailing commit으로 반영한 뒤 최신 CI와
  mergeability를 확인한다. 상세 근거는 [PR #4266 검토](../pr/archives/pr_4266_review.md)에 남긴다.

## Issue #4268 - 컨트리뷰터 PR 성능 검증 책임 경계

- PR 제출 자격으로 특정 로컬 환경의 절대 성능 수치, 비공개 코퍼스 또는 메인테이너 전용
  벤치마크 통과를 요구하지 않는 정책 제안을 검토했다.
- 성능 회귀 자체를 면제하는 것으로 오해하지 않도록 공개된 결정적 성능 래칫과 GitHub required
  checks는 merge gate로 유지하고, 환경 의존 최종 판정은 메인테이너 책임으로 분리한다.
- 중복 이슈 검색 뒤 [#4268](https://github.com/edwardkim/rhwp/issues/4268)을 등록하고 assignee
  `edwardkim`, milestone `v1.0.0`, labels `documentation`·`performance`를 지정했다.
- 최신 `upstream/devel` `fc8eb90d6`에서 `docs/issue-4268-contributor-performance-policy` 브랜치를
  만들었다. [수행계획](../plans/task_m100_4268.md)에 따라 공개 기여 가이드, PR 템플릿과 maintainer
  검증 문서를 일관되게 갱신한다.
- `CONTRIBUTING.md`에는 제출 책임 경계, PR 템플릿에는 선택형 성능 영향 기록란,
  `local_validation.md`에는 maintainer의 통제 환경 재검증과 blocking 근거 의무를 반영했다.
- 공개된 결정적 성능 회귀 테스트와 required checks는 그대로 유지하며, CI timeout이나 비공개
  벤치마크의 절대 수치를 공통 제출 기준으로 바꾸지 않았다.
- 문서 링크 검사와 `git diff --check`를 통과했다. 코드·workflow·WASM·렌더러 변경이 없는 문서 전용
  작업이므로 Cargo, WASM 및 시각 검증은 수행하지 않는다.
- 구현과 검증 결과는 [Stage 1 기록](../working/task_m100_4268_stage1.md)에 정리했다. 후보 커밋 준비 뒤
  별도 승인을 받아 Open PR 생성 단계로 넘어갔다.

## PR #4269 - 컨트리뷰터 PR 성능 검증 책임 경계

- 작업지시자 승인 뒤 [PR #4269](https://github.com/edwardkim/rhwp/pull/4269)을 `devel` 대상 Open PR로
  생성하고 assignee `edwardkim`, milestone `v1.0.0`, labels `documentation`·`performance`를 지정했다.
- 작업지시자는 reviewer를 `edwardkim`으로 지정해 maintainer self-review를 승인했다. GitHub는 작성자의
  자기 PR reviewer request를 허용하지 않아 REST 요청이 HTTP 422로 거부됐으므로 별도 review request는
  등록하지 않고 [PR 검토 기록](../pr/archives/pr_4269_review.md)에 담당자와 플랫폼 제한을 명시했다.
- review candidate `0c1717f40`의 6개 문서 변경을 검토해 blocking finding이 없음을 확인했다. Markdown
  링크 검사와 `git diff --check`가 통과했고 코드·렌더·fixture 영향이 없어 Cargo·WASM·시각 검증은 생략했다.
- root `CONTRIBUTING.md`와 `.github` PR 템플릿이 review-only fast-pass B 허용 경로 밖이므로, review 기록을
  포함한 최신 head의 일반 CI 성공과 self-review `COMMENT` 게시 승인·merge 승인을 별도 게이트로 둔다.

## PR #4271 - #4162 글자 서식 no-op + #4270 문서 전환 예약 누수

- 캐럿·F5 셀 블록 선택 상태에서 툴바 글자 색/글꼴/크기가 `hasSelection()` 계열 가드에 걸려
  조용히 no-op이던 결함(#4162)을 캐럿 대기 글자 모양(pending char shape) 예약 메커니즘으로
  고쳤다. `InsertTextCommand`가 예약 서식을 삽입 range에 직접 적용하고, IME 조합은
  `applyPendingCharShapeToRange`로 별도 적용한다. `getSelectionOrdered()`가 anchor만 있어도
  non-null을 돌려줘 빈 range로 조용히 no-op되던 별도 결함도 `getNonEmptySelection()`으로
  같이 닫았다.
- 구현 뒤 gestell skill로 자체 리뷰해 `deactivate()`/`dispose()`가 새 예약 상태를 리셋하지
  않는 결함을 발견했다 — `window.__inputHandler.deactivate()`를 직접 호출해 수정 전 필드가
  안 지워짐을 실행으로 재현했다. 새 이슈 [#4270](https://github.com/edwardkim/rhwp/issues/4270)으로
  등록하고 같은 PR에서 함께 고쳤다.
- 구현 중 시도했던 `command-feedback.ts`(실행 불가 커맨드 가시화 helper)는 실제 커맨드
  호출부를 하나도 바꾸지 않아 죽은 코드였고, `integration/all-works`에 같은 목적의 더 완성된
  구현이 이미 있어 충돌을 피하려고 제거했다.
- contributor 후보의 TypeScript·studio 818건 검증 뒤 메인테이너 fresh Node WASM에서 삽입
  undo가 오른쪽 원문 서식을 오염시키는 추가 결함을 발견했다. Rust 문단 삭제 경계를
  `034df2ec0`으로 보정하고 unit test 2건과 Studio undo baseline 단언을 추가했다.
- 최신 `devel`을 양쪽 기능을 보존해 `edc1869ea`로 통합한 뒤 Rust nextest 5,486건, Studio
  823건, Docker web·Node WASM, TypeScript, fmt·clippy를 통과했다. 최종 PR head
  `06d3ec7e5`의 CI `31291755667`, CodeQL `31291755574`, Render Diff `31291755573`도 모두
  통과했고 [승인 review](https://github.com/edwardkim/rhwp/pull/4271#pullrequestreview-4890430796)를 게시했다.
- [PR #4271](https://github.com/edwardkim/rhwp/pull/4271)은 merge commit `d4834d737`로
  `devel`에 반영됐다. 기본 branch 차이로 merge 직후 #4162·#4270은 OPEN이었고, archive
  운영 기록 반영 뒤 승인된 후속 절차에서 수동 종료한다.

상세 근거: [PR #4271 메인테이너 검토](../pr/archives/pr_4271_review.md),
[contributor 최초 검토](../pr/archives/pr_4271_contributor_review.md).

## Issue #4272 - 중첩 표 안쪽 셀 텍스트 선택 하이라이트

- 최신 `devel` web WASM과 실제 rhwp-studio에서
  `samples/basic/issue2007_nested_cell_pagination_42065.hwp` 물리 5쪽의 중첩 표 텍스트 선택을
  재현했다. 깊이 3 논리 selection은 생성되지만 선택 하이라이트가 0개인 결함이다.
- 동일 증상 선행 검색 뒤 [#4272](https://github.com/edwardkim/rhwp/issues/4272)를 등록하고 assignee
  `edwardkim`, milestone `v1.0.0`, 관련 bug·Studio·table·API·rendering·test 라벨을 지정했다.
- 최신 `upstream/devel` `828eabc19`에서 `fix/issue-4272-nested-cell-text-selection` 브랜치를 만들었다.
  [수행계획](../plans/task_m100_4272.md)에 따라 평면 셀 선택 API를 전체 `cellPath` 기반으로 확장하고
  브라우저 hot path의 추가 전체 순회 없이 래칫 테스트와 시각 증적을 남긴다.
- 전체 경로 Rust/WASM 선택 rect API와 Studio 라우팅을 구현했다. 실제 샘플 Rust 래칫 1건,
  경로 매처 2건, Studio 전체 816건, TypeScript와 형식 검사가 통과했다.
- 현재 task branch의 메인 작업공간에서 표준 Docker WASM을 다시 빌드해 실행 중인 dev 서버에
  반영했다. 호스트 Chrome CDP 실제 마우스 드래그에서 `23,504` 전체 선택, 하이라이트 1개와
  브라우저 경고·오류 0건을 확인했고, 인접 #4252 E2E도 통과했다.
- 구현·검증·절차 교정 내용은 [Stage 1 기록](../working/task_m100_4272_stage1.md)에 정리했다.
  후보 커밋과 이후 PR 단계는 승인 게이트에 따라 진행한다.
- Stage 1 시각 판정 중 같은 중첩 셀 선택을 Ctrl+C 하면 평면 셀 복사 API가 바깥 셀의 빈 문단을
  읽어 내부 클립보드가 빈 문자열이 되는 후속 RED를 확인했다. 작업지시자 판정에 따라 이를 별도
  이슈로 분리하지 않고 #4272의 필수 사용자 여정인 선택 → 복사 → 붙여넣기 Stage 2로 처리한다.
- plain text 복사와 HTML export에 전체 `cellPath` API를 추가하고 Studio의 중첩 셀 Ctrl+C를
  라우팅했다. 실제 샘플 Rust 래칫, Studio 전체 817건, TypeScript·형식·매니페스트가 통과했다.
- 현재 task branch에서 Docker WASM을 다시 빌드해 dev 서버에 반영했다. 호스트 Chrome CDP 실제
  mouse drag → Ctrl+C → Ctrl+V 결과 `23,504`가 보존됐고, 인접 #4252와 기존 일반 copy-paste
  E2E도 통과했다. 상세 결과는 [Stage 2 기록](../working/task_m100_4272_stage2.md)에 남긴다.
- 작업지시자 요청으로 물리 11쪽 자식 표 셀도 추가 검증했다. 최내곽 문단 22 대신 평면 호환 필드
  0을 넘기는 후속 RED를 발견해 `cellParaIndexOf()`로 교정했다. offset `66 -> 89`의 실제 선택
  문자열 복사와 path API 문단 인자 22를 CDP 래칫으로 고정했다.
- 작업지시자가 깊이 3 자식 표 객체 선택 후 Ctrl+C 실패를 추가로 확인했다. CDP RED에서 선택
  참조의 full path는 정확했지만 전체 path로 셀 문단까지 내려간 뒤 바깥 호환 `ci=1`을 조회해
  `컨트롤 1 범위 초과`가 발생했다. #4272 Stage 3에서 선택 표의 owner prefix path와 마지막
  control index를 분리해 키보드·커맨드 복사 경로를 함께 교정한다.
- `tableObjectClipboardTarget()`이 마지막 path 엔트리의 `controlIndex=0`과 앞쪽 depth 2 owner
  path를 분리하도록 구현했다. Ctrl+C와 `performCopy()`가 같은 계산을 사용하며 본문 표의 빈 path
  계약은 유지한다.
- 실제 샘플 native 래칫 2건, Studio 전체 819건, TypeScript·형식 검사와 호스트 Chrome CDP를
  통과했다. 물리 5쪽 표 객체 복사 결과는 내부 클립보드 `[표]`, control 포함, warning/error 0건이며
  #4252 객체 선택과 #4272 물리 5·11쪽 텍스트 복사 회귀도 통과했다. 상세 결과는
  [Stage 3 기록](../working/task_m100_4272_stage3.md)에 남긴다.
- 물리 4쪽 깊이 3 자식 표를 새 문서에 붙여넣을 때 셀 스타일이 손실되는 후속 결함은 동일 문서
  native 붙여넣기와 달리 교차 문서 HTML export/import에서 발생함을 확인했다. 동일 증상 선행 검색
  뒤 작업지시자 결정에 따라 [#4275](https://github.com/edwardkim/rhwp/issues/4275)로 분리하고
  assignee `edwardkim`, milestone `v1.0.0`, bug·Studio·table·API·test 라벨을 지정했다.
- #4272는 중첩 셀 선택·텍스트 복사와 붙여넣기·중첩 표 객체 복사까지 로컬 구현과 focused 검증을
  완료했다. 정정 commit이 아직 `devel`에 통합되지 않았으므로 GitHub 이슈는 열린 상태로 유지하고,
  전체 PR 검증·원격 push·PR 생성의 별도 승인 게이트로 넘긴다.
- 작업지시자 승인 뒤 최신 `upstream/devel` `e4d07fab7`을 task branch에 병합하고 전체 PR 게이트를
  재실행했다. 새 devel의 invalidation guard에는 경로 기반 복사 API를 session-state 전용으로
  명시해 focused guard 5/5와 전체 nextest 5,484/5,484를 통과했다.
- release build·lib 3,361건·native Skia 3종·format·Clippy·doctest·TypeScript와 Studio 전체
  819건이 통과했다. 표준 Docker WASM을 다시 빌드해 `pkg`에 반영했고 #4272의 텍스트 선택·물리
  11쪽 복사·3중 표 객체 복사 headless E2E 3종도 최종 통과했다.
- 최종 검증 기록을 후보 commit으로 고정한 뒤 승인된 원격 push와 `devel` 대상 Open PR 생성을
  진행한다. reviewer 지정·검토 기록·merge와 이슈 close는 다음 승인 게이트로 유지한다.

## PR #4276 - 중첩 셀 선택·복사를 전체 경로로 처리

- 작업지시자 승인 뒤 code candidate `7da15f346`을 원격 source branch에 push하고
  [PR #4276](https://github.com/edwardkim/rhwp/pull/4276)을 `devel` 대상 Open PR로 생성했다.
- 1차 트리야지로 assignee `edwardkim`, milestone `v1.0.0`, labels `bug`·`rhwp-studio`·`api`·
  `rendering`·`test`·`table`을 적용했다. `gh pr edit`의 Projects Classic GraphQL 오류는 같은 명령을
  반복하지 않고 PR issue REST endpoint로 처리한 뒤 세 필드를 재조회했다.
- 작업지시자는 작성자 `edwardkim`의 maintainer self-review를 승인했다. GitHub는 자기 PR reviewer
  request를 허용하지 않으므로 별도 request를 만들지 않고 빈 requested reviewer 목록을 확인했다.
- +1,998/-101의 대형 PR로 분류해 즉시 admin merge하지 않는다. 코드·API·성능 경로를 별도 cycle로
  검토했고 blocking finding이 없음을 확인했다. code candidate의 CI·CodeQL·Canvas visual diff는 모두
  성공했다.
- [PR 검토 기록](../pr/archives/pr_4276_review.md)과 대표 시각 asset을 같은 source branch의
  review-only 후속 commit으로 반영한다. 최신 head의 fast-pass·mergeability 확인, self-review
  `COMMENT` 게시와 merge는 각각 다음 승인 게이트로 유지한다.

## PR #4311 - 기술 문서 메타데이터 역할 정합화

- 저장소 전체 메타데이터 검사에서 발견된 기존 기술 문서 오류 3건을 검토했다.
  `envelope_provenance.md`는 단일 진실 공급원인 `canonical`로, #3604 비밀번호 암호화 C++
  검토 문서는 지속되는 구현 선택인 `decision`으로 분류하고 자체 canonical 경로를 추가했다.
- 새 kind를 도입하지 않았고 기술 사실을 재검증한 변경도 아니므로 기존 `last_verified` 값은
  유지했다. 메타데이터 검사 520개, Markdown 상대 링크 검사 525개와 `git diff --check`가
  모두 통과했다.
- [PR #4311](https://github.com/edwardkim/rhwp/pull/4311)을 `devel` 대상 Open PR로 생성하고
  assignee `edwardkim`, milestone `v1.0.0`, label `documentation`을 지정했다.
- 전체 diff가 `mydocs/` 아래에만 있는 review-only fast-pass B 대상이다. 상세 판정은
  [PR #4311 검토](../pr/archives/pr_4311_review.md)에 남기고 최신 head의 CI·mergeability를
  확인한 뒤 maintainer self-review와 merge를 진행한다.

## PR #4317 - #4314 후속 roadmap 사실과 완료 상태 보정

- 통합 PR #4314 머지 뒤 남아 있던 문서 사실 오류를 최신 `upstream/devel` `b64c42f37` 기준으로
  재검토했다. #4110·#4111은 이슈이고 실제 recipe merge PR은 #4182·#4183이며, #4314에 착지한
  R84·R93은 `[완료]`가 맞다.
- code candidate `3936d6002`에서 roadmap 집계를 완료 35·실측 10·문서 7·이슈 3·가설 45로
  재생성했다. OLE contract script 변환과 fallback 경계, HWPX 구현 현황 표의 7개 항목 및
  잘못된 함수 수치도 source 근거와 일치하도록 바로잡았다.
- roadmap·Markdown 링크·문서 metadata 검사와 `git diff --check`가 모두 통과했다. 변경은
  `mydocs/` 아래 6개 파일(+31/-24)뿐이라 Cargo·WASM·시각 검증은 적용하지 않았다.
- [PR #4317](https://github.com/edwardkim/rhwp/pull/4317)을 `devel` 대상 Open PR로 생성하고
  assignee `edwardkim`, milestone `v1.0.0`, label `documentation`을 지정했다. 최초 head의
  fast-pass와 `MERGEABLE/CLEAN`도 확인했다.
- 작업지시자는 작성자 `edwardkim`의 maintainer self-review를 승인했다. 작성자 자기 PR에는
  reviewer request와 `APPROVE`를 시도하지 않고, [검토 기록](../pr/archives/pr_4317_review.md)을
  trailing review-only commit으로 반영한다. 최신 head CI 확인과 `COMMENTED` review 게시까지
  진행하며 merge는 별도 승인 게이트로 유지한다.
