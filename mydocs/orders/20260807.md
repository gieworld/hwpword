# 오늘 할 일 - 2026년 8월 7일

## 공통 — 운영 작업

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #4080 | 캐시 고아 정리 수동 실행과 `main` 반영 요청 | 진행 | 수동 dispatch 로 9.07→6.57GiB(97.4%→65.7%). `main` 반영은 메인테이너가 직접 처리 |
| #4040 | 파일 게이트 native-skia test 3건 CI 회복 | 진행 | 로컬 구현·검증 완료, PR 생성 대기 |
| #4132 | 함수 단위 native-skia 게이트 test 분리 등록 | 등록 | #4040 선행 후 착수 |
## PR #4177 - mydocs 충돌 merge의 녹색 source head 재사용

- [이슈 #4176](https://github.com/edwardkim/rhwp/issues/4176)의 범위로, latest current-base merge의 직접
  source parent가 같은 PR source의 녹색 candidate이면 source parent가 과거 merge여도 재사용하도록 CI,
  CodeQL, Render Diff preflight를 보완했다. 실제 #4136과 #4165의 today 충돌 수동 해소 경로는 모두
  `mydocs/` 하나다.
- trusted remerge diff, 동일 PR source·candidate identity, 녹색 aggregate를 모두 확인한다. source·test·
  fixture·PDF·golden·baseline 충돌 해소, identity 불일치, 녹색 후보 부재는 이전처럼 Full CI로 fallback한다.
- CI 실행 경로(`.github/workflows/**`, `.github/actions/**`, CI impact classifier, merge-resolution
  verifier)를 바꾼 source PR은 `current-base-source-ci-execution-change`으로 거부해 Full CI를 강제한다.
  #4170이 이 경계에 해당하며, CI 자체 변경을 review-only 재사용으로 검증하지 않는다.
- workflow 계약 36건, actionlint, Markdown 링크·diff 검사를 통과했고 code head `8119074ce`의 GitHub CI
  전체, CodeQL, Canvas visual diff도 모두 성공했다.
- 이 review·오늘할일 trailing commit `cfe6a1ccc`는 code head `8119074ce`의 녹색 candidate를 재사용해
  fast-pass됐다. CI·CodeQL·Render Diff preflight와 aggregate는 성공했고 heavy job, CodeQL 분석,
  Canvas visual diff는 모두 skip됐다.
- 후속 문서 정정 `b3d328ce5`에서는 중간 fast-pass 후보의 Canvas `skipped`를 실패로 처리해 이전 실제
  Canvas 성공 후보까지 탐색하지 않는 오류를 확인했다. Stage 2는 `skipped` 후보를 건너뛰고 같은 PR
  identity의 더 이전 Canvas 성공 후보를 계속 탐색하도록 보정했다. workflow 계약 37건, actionlint,
  Markdown 링크·diff 검사를 통과했다. code head `f571572a9`의 GitHub CI 전체, CodeQL, Canvas visual
  diff도 모두 성공했다.
- direct source-parent와 trusted remerge bridge는 사용자가 별도 mydocs-only current-base 병합 PR의 수동
  conflict resolve로 검증한다.

상세 근거: [PR #4177 검토](../pr/archives/pr_4177_review.md).

## PR #4173 - mydocs 충돌 해소 병합의 review-only fast-pass 재사용

- [이슈 #4172](https://github.com/edwardkim/rhwp/issues/4172)의 범위로, current-base merge에서 자동
  merge tree가 충돌해도 수동 해소 경로 전체가 `mydocs/` 아래이면 같은 PR source의 녹색 candidate를
  재사용하도록 CI, CodeQL, Render Diff preflight를 보완했다.
- 병합은 정확히 두 parent이고 current base가 한 parent여야 한다. source·test·workflow·sample·PDF·golden·baseline
  충돌 해소, 경로 조회 실패, 복수 current-base merge는 계속 full CI로 fallback한다. 검사는 PR head가 아닌
  current base tree에서 읽어 실행한다.
- 실제 [#4136](https://github.com/edwardkim/rhwp/pull/4136)의 today add/add 충돌 merge에서
  `mydocs/orders/20260807.md` 하나만 수동 해소됐음을 확인했고, 허용·거부 workflow 계약 34건,
  actionlint, diff check를 통과했다.
- 최초 trailing review 기록에서 Render Diff 후보 결과 조회가 candidate 탐색 반복문 안의 `break` 뒤에
  남아 Canvas 전체 경로로 떨어지는 제어 흐름 오류를 확인했다. 최종 code head `b1ecf57e9`은 조회를
  반복문 밖으로 옮기고, 위치 계약과 추출 GitHub Script의 Node 문법 검사를 추가했다.
- `b1ecf57e9`의 GitHub CI 전체, CodeQL, Render Diff가 모두 성공했다. 이 review·오늘할일은 trailing
  documentation commit으로 push한 뒤 최신 head의 CI·CodeQL·Render Diff fast-pass aggregate를
  재확인하고, 작업지시자 지시에 따라 병합한다.

상세 근거: [PR #4173 검토](../pr/archives/pr_4173_review.md).

## PR #4178 - 누름틀 직렬화와 WebHwpCtrl P0 하니스

- [PR #4178](https://github.com/edwardkim/rhwp/pull/4178)의 누름틀 직렬화 회귀와 WebHwpCtrl P0
  오라클 하니스를 검토했다. 기여자 원 commit은 변경하지 않았다.
- 메인터너 보정으로 한글 2022 comma/dotted 버전 판정, stale 오라클 산출물 격리, 기존 사용자 한글
  프로세스 비종료, ParameterSet Item 521 계약, COM Quit 뒤 비동기 종료 settle 대기를 추가했다.
- 실제 Windows 10 RDP 대화형 session에서 한글 `12, 0, 0, 535` COM 오라클과 `field-read` 전체 gate를
  실행해 exit 0, 호출 8건, 잔류 Hwp/HwpFrame/python 프로세스 없음을 확인했다. P0 L2 차등 결과는
  MATCH 3, VALUE_DIFF 2, MISSING_API 3이며 L3 저장 검증 대상은 없다.
- 보정 code head `7f38e41ef`의 GitHub Full CI와 CodeQL이 모두 성공했다. 이 review·오늘할일 trailing
  commit의 fast-pass aggregate와 mergeable 상태를 재확인한 뒤 작업지시자 승인에 따라 merge와 후속 처리를
  진행한다.

상세 근거: [PR #4178 검토](../pr/archives/pr_4178_review.md),
[메인터너 보정 기록](../pr/archives/pr_4178_review_impl.md).

## PR #4171 - johndoekim CFB CLSID·HWP3 상대 크기 통합

- [PR #4171](https://github.com/edwardkim/rhwp/pull/4171)에 @johndoekim의 #4144를 먼저, #4160을
  그 다음으로 최신 `devel` 위에 통합했다. #4160이 #4144의 CLSID 보존을 선행 의존성으로 명시하므로
  해당 순서를 적용했고, 원 contributor head는 변경하지 않았다.
- #4144는 mini CFB 재포장과 HWP3 OLE 서브 스토리지 승격에서 OLE 루트 CLSID를 보존한다. #4160은
  HWP3 변환 결과의 미지정 CharShape 상대 크기를 OWPML 기본값 `100`으로 적용해 한컴의 극소 본문
  해석을 막는다.
- 통합 검토에서 CFB v4의 루트 CLSID 오프셋이 v3 전용 식으로 계산되는 문제를 발견했다. 메인터너 보정
  `c4f85c0a0`은 공통 512바이트 헤더 뒤의 `512 + SID * sector_size` 계산으로 고치고 v4 회귀를
  추가했다. v3 writer나 원 contributor의 보존 정책은 바꾸지 않았다.
- CFB v4 focused 1건, #4097 계약 3건, #4141 계약 5건, 전체 `cargo test --profile release-test --tests`,
  fmt, clippy, WASM library check가 통과했다. code head의 CI, CodeQL, Render Diff도 모두 성공했다.
- 이 review·오늘할일은 code candidate 뒤의 trailing documentation commit이다. 최신 head의
  review-only aggregate와 merge 가능 상태를 재확인하고 작업지시자 승인 뒤 병합하면 `Closes #4097`,
  `Closes #4141`의 자동 종료와 원 PR #4144·#4160의 후속 정리를 확인한다.

상세 근거: [PR #4171 검토](../pr/archives/pr_4171_review.md),
[통합·메인터너 보정 기록](../pr/archives/pr_4171_review_impl.md).

## PR #4166 - jeong-sik Studio 문단·표 경계 통합

- [PR #4166](https://github.com/edwardkim/rhwp/pull/4166)에 @jeong-sik의 #4120, #4133,
  #4134를 최신 `devel` 기준으로 순서대로 통합했다. 원 contributor head는 변경하지 않았다.
- #4120은 머리말/꼬리말 문단 상태를 툴바·눈금자 reader와 통일하고 Fixed·Minimum·SpaceOnly
  줄 간격에서 이전 선택값을 남기지 않는다. #4133은 반올림 경계에서 중복된 표 세로 경계를
  병합해 드래그 범위가 한 방향으로 뒤집히는 문제를 해결한다.
- #4134의 머리말/꼬리말·각주 문단 서식 적용은 실제 Chromium에서 값 변경과 Undo는 성공했지만
  Undo 뒤 편집 모드가 이탈했다. 메인터너 보정 `d2a0f2e6f`은 일반 snapshot 의미를 바꾸지 않고
  두 경로에만 `SubmodeSnapshotCommand`를 사용해 Undo/Redo 후 같은 편집 문맥을 유지한다.
- Studio focused 35건, 전체 `npm test` 802건, TypeScript, production build와 headless Chromium
  툴바·머리말·각주 왕복을 통과했다. code head의 CI, CodeQL, Render Diff도 성공했다.
- 이 review·오늘할일 문서 commit을 trailing documentation commit으로 push한 뒤 최신 head의
  fast-pass 또는 fallback CI aggregate를 확인한다. 작업지시자 승인 뒤 병합하면 `Closes #4109`의
  자동 종료와 원 PR #4120·#4133·#4134의 후속 정리를 확인한다.

상세 근거: [PR #4166 검토](../pr/archives/pr_4166_review.md),
[통합·메인터너 보정 기록](../pr/archives/pr_4166_review_impl.md).

## PR #4156 - humdrum00001010 누적 PR 통합

- [PR #4156](https://github.com/edwardkim/rhwp/pull/4156)을 merge commit
  `ff038699f4d2efea199e51b3f6dae4d88930a8aa`로 `devel`에 통합했다.
- 원 기여 변경 #4124, #4127, #4131을 순서대로 반영했고, 메인터너 보정으로 교차 출처 열기
  폴백의 실제 `file:open` 경로 회귀 검증을 추가했다.
- 최신 head에서 CI, CodeQL, Render Diff가 모두 성공했다. 로컬에서는 전체
  `cargo test --profile release-test --tests`, Native Skia, focused Rust, Studio 단위·빌드,
  WASM build와 Chromium 폴백 동작을 확인했다.
- `Closes` 연동 대상 #4123, #4126, #4128, #4129의 자동 close 상태와 원 기여 PR 후속 comment·close는
  이 기록 PR merge 뒤 확인한다. 남은 코드 작업은 없다.

상세 근거: [#4124 검토](../pr/archives/pr_4124_review.md),
[#4127 검토](../pr/archives/pr_4127_review.md),
[#4131 검토](../pr/archives/pr_4131_review.md).

## #536 - Canvas2D 한컴 사각 안 숫자 PUA parity

- [PR #4122](https://github.com/edwardkim/rhwp/pull/4122)의 17쪽 pagination 수정 위에
  `task_m100_536_canvas2d_boxed_pua` stacked branch를 만들었다. 자식 PR의 base는
  `task_m100_4069`이며, #4122 merge 뒤 `devel`로 retarget한다.
- 작업지시자가 `issue2007_nested_cell_pagination_42065.hwp`의 정상 한컴 PDF를 교체했다.
  새 정답지 SHA-256은 `9b0390f856bb9ad43337679babf6677209b7c7ab678b6616fcc6d6d5551ff1c4`다.
- 대상 `U+F02B1`은 `CharOverlap`이 아니라 raw `TextRun`이다. CanvasKit의 결정적 사각형+숫자
  합성 경로와 달리 기본 Canvas2D가 브라우저 글꼴에 raw PUA를 맡긴 것이 근인이다.
- Canvas2D에 `U+F02B1..U+F02C4` bounded vector fallback을 추가하고 실제 HWP 물리 2쪽 E2E를
  고정했다. 17쪽·raw PUA·사각 외곽·내부 숫자 잉크가 통과했고 표준 WASM build도 성공했다.
- 작업지시자 승인 뒤 release library 3,286 passed, 전체 `release-test --tests`, Native Skia
  58+2+4, fmt·diff·clippy·doc test, TypeScript, Studio 763 tests와 최종 WASM/E2E를 통과했다.
  [stacked Draft PR #4139](https://github.com/edwardkim/rhwp/pull/4139)를 생성했고 author
  assignee, milestone `v1.0.0`, `bug`·`rust`·`javascript`·`rhwp-studio`·`rendering`·`test`
  labels로 1차 트리야지를 완료했다. 작업지시자 최종 시각 판정도 통과했으며, 현재 상태는 #4122
  선행 merge와 #4139의 `devel` retarget·CI 대기다.

상세 근거: [#536 Stage 1](../working/task_m100_536_stage1.md),
[#4139 검토](../pr/archives/pr_4139_review.md).

## PR #4153 - OVR·편집 스윕 산출물 경로 통일

- [PR #4153](https://github.com/edwardkim/rhwp/pull/4153)의 원 기여 commit `06989c41f`은
  OVR 기본 산출물 경로를 저장소 루트의 `.gitignore`된 `output/ovr`로 정하고 CONTRIBUTING 예시를
  같은 경로로 통일했다.
- 이슈 [#4152](https://github.com/edwardkim/rhwp/issues/4152)의 수용 범위와 대조해
  `examples/edit_sweep.rs`와 `mydocs/manual/verification/edit_sweep.md`에 남은 `out/sweep` 안내를
  메인터너 보정 `3a7c88b13`으로 `output/sweep`으로 통일했다. 일반 CLI의 별도 `out/` 관례는
  변경하지 않았다.
- `git diff --check`, Markdown 링크 검사, Python 문법·도움말, `.gitignore` 경로 확인, `cargo fmt --check`,
  최신 `devel` merge simulation을 통과했다. `target/release/rhwp`가 없어 OVR 전체 실행은 하지 않았다.
- 보정 code head의 GitHub Actions full CI·CodeQL·Native Skia·네 test shard가 모두 성공했다. 이 review와
  오늘할일 문서는 동일 source branch에 review-only trailing commit으로 추가했으며, 최신 head의
  fast-pass aggregate와 merge 가능 상태를 다시 확인한다.
- source보다 최신인 `upstream/devel` today 기록을 source에 복사하면 archive 링크가 깨질 수 있음을
  확인했다. 현재 PR 항목만 변경되지 않은 section 경계에 추가하고 merge tree 링크 검사를 수행하도록
  PR workflow와 collaborator 외부 PR 가이드를 보강했다.

상세 근거: [PR #4153 검토](../pr/archives/pr_4153_review.md),
[메인터너 보정 기록](../pr/archives/pr_4153_review_impl.md).

## kevin9327 누적 PR 검토 - #4105, #4106, #4108, #4114, #4116

- 초기 `upstream/devel` `9f564bbe` 위에 다섯 PR의 원 commit을 순서대로 누적 cherry-pick했고 충돌은 없었다.
- #4106의 착수 잠금은 단순 `착수` 단어 인용을 잠금으로 오인할 수 있어, `착수합니다 — <범위>` 형식만
  인정하도록 메인터너 보정과 Python 회귀 2건을 추가했다.
- #4105의 protocol, #4108의 R94와 기계 집계, #4114의 R7, #4116의 R74·R79을 누적 구현 상태와
  정렬했다. README 생성 결과는 `완료 31 · 실측 9 · 문서 7 · 이슈 4 · 가설 49 = 100`이다.
- CI 워크플로 변경은 작업지시자 지시에 따라 별도 revert했다. Python·문서 보정 뒤 Cargo 전체 회귀는
  실행하지 않았으며, 실행한 focused Python/Rust 계약, fmt, clippy, actionlint는 모두 통과했다.
- 최초 #4140 node-binding CI는 capabilities 34개 선언과 Node 31개 생성 타입의 drift, 세 wrapper 누락을
  발견했다. 메인터너 보정으로 `export-plan-schema`·`export-agent-manifest`·`explain` wrapper와 argv
  회귀를 추가하고 생성 타입을 재생성했다. 실제 Node 17 files·445 tests, typecheck, build, gen:check를
  통과했다.
- #4140과 보정 head는 원격에 push했다. 이 파일의 #536 기록과 add/add 충돌을 보존하도록 최신
  `upstream/devel` `a5698255e`를 merge했고, 새 head의 CI·CodeQL 성공을 확인한 뒤 merge한다.

상세 근거: [누적 구현·검토 기록](../pr/archives/pr_kevin9327_20260807_review_impl.md),
[PR #4105](../pr/archives/pr_4105_review.md), [PR #4106](../pr/archives/pr_4106_review.md),
[PR #4108](../pr/archives/pr_4108_review.md), [PR #4114](../pr/archives/pr_4114_review.md),
[PR #4116](../pr/archives/pr_4116_review.md).

## PR #4148 - Codex 프로젝트 메모리 덤프 활성화

- 기존 redirect stub이던 `mydocs/manual/codex/MEMORY.md`를 작업지시자가 확정한 장기 운영 규칙의
  활성 진입점으로 전환하고, 이전 redirect는 historical archive로 보존했다.
- `AGENTS.md`, 루트 canonical manifest와 manual·Codex 문서 지도의 역할·로딩 순서를 같은 계약으로
  정렬했다.
- 최신 `devel` 기준 문서 전용 브랜치 `docs/codex-memory-20260807`을 만들고
  [Draft PR #4148](https://github.com/edwardkim/rhwp/pull/4148)을 생성했다. author assignee,
  milestone `v1.0.0`, `documentation` label로 1차 트리야지를 완료했다.
- 변경분 링크·redirect 검사와 6개 문서 targeted link check, `git diff --check`가 통과했다. 전체 metadata
  검사는 이번 diff 밖의 기존 `mydocs/tech` 오류 3건만 재현했다.
- tracked `output/poc` 삭제 커밋은 원래 브랜치에 보존해 이 PR에서 제외했고, PDF도 최신 `devel`과
  동일한 SHA-256이어서 포함하지 않았다.

상세 근거: [#4148 검토](../pr/archives/pr_4148_review.md).

## #4158 → #4159 순차 처리

- 작업지시자 지시에 따라 [#4158](https://github.com/edwardkim/rhwp/issues/4158)의 실제
  `CharOverlap` 사각 숫자 tofu 결함을 먼저 처리하고, 해당 단계를 커밋한 뒤
  [#4159](https://github.com/edwardkim/rhwp/issues/4159)의 종료 재귀 중첩 표 bottom 테두리 clip
  결함을 별도 단계로 처리한다.
- #4158은 최신 `upstream/devel` `5119ea498`에서
  `task_m100_4158_char_overlap_boxed_pua` 브랜치를 만들었다. Canvas2D·SVG·Native Skia가
  동일한 `U+F02B1..U+F02C4` 사각 숫자 의미를 사용하도록 하고, 실제 HWP 물리 10쪽 WASM
  Canvas2D 래칫을 남긴다.
- #4158 구현·집중 검증·시각 증적과 단계 커밋 전에는 #4159 소스를 변경하지 않는다. 두 이슈의
  전체 PR 검증과 원격 작업은 각 단계의 집중 검증 결과를 보고한 뒤 별도 승인 절차를 따른다.
- 작업지시자 승인 뒤 #4158의 release build, library 3,292건, 전체 integration, Native Skia
  공식 3종, Clippy·fmt·doc, Studio TypeScript·Node 22 테스트 765건, release WASM과 브라우저
  래칫까지 모두 통과했다. 원격 작업 없이 #4158 로컬 단계를 종료하고 별도 #4159 브랜치로
  전환한다.

상세 근거: [#4158 수행계획](../plans/task_m100_4158.md).

## PR #4119 - F5 셀 블록 직접 서식과 빈 블록 fallback 차단

- [PR #4119](https://github.com/edwardkim/rhwp/pull/4119)의 contributor 원 head
  `2a874d79d`에서 F5 셀 블록을 텍스트 selection과 별도로 판정해, 문단·글자 서식이 선택한
  모든 셀에 적용되도록 보완했다.
- 메인터너 보정 `33dfd6e3a`은 Ctrl+클릭으로 모든 셀을 제외한 빈 블록을 일반 커서로 fallback하지
  않게 했다. 문단 서식은 빈 대상 목록으로 no-op이며 글자 서식도 변경·undo history 없이 종료한다.
- Studio focused test 16건, TypeScript, `npm test` 779건, 최신 `devel` merge simulation과 production
  build를 통과했다. Chromium 2x2 표에서 정상 블록은 4/4 적용·Undo 원복, 전체 제외 블록은 값 변경과
  history 생성이 모두 0건임을 확인했다.
- @jeong-sik은 아직 merge된 rhwp PR이 없는 첫 병합 기여 후보다. 최신 `upstream/devel`
  `5119ea498`을 source에 병합해 오늘 기록 add/add 충돌을 사전에 해소했다. `33dfd6e3`의 같은 PR/source
  녹색 CI·CodeQL·Render Diff와 current-base merge tree가 일치하면 뒤의 review-only 기록은 fast-pass
  aggregate를 재사용할 수 있고, 그렇지 않으면 full CI fallback을 확인한 뒤 merge한다.

상세 근거: [PR #4119 검토](../pr/archives/pr_4119_review.md),
[메인터너 보정 기록](../pr/archives/pr_4119_review_impl.md).

## #4080 - 캐시 스윕이 `main` 정의로 돌아 고아 정리가 발동하지 않았다

- [PR #4082](https://github.com/edwardkim/rhwp/pull/4082) merge 뒤 첫 정기 스윕
  ([run 31134261783](https://github.com/edwardkim/rhwp/actions/runs/31134261783))이 삭제 0건으로 끝났다.
  로그가 `GiB` 가 아닌 `GB` 표기였고 고아 정리 줄이 없어 확인하니 `head_branch=main` 이었다.
- `schedule` 이벤트는 기본 브랜치 정의로만 실행된다. `devel` 은 `main` 보다 1,110커밋 앞서 있어
  #4082 의 변경이 정기 스윕에 반영되지 않는다. 같은 함정이 #3508 에서 실측되고 #3812 제목에도
  적혀 있었는데 #4082 작업에서 확인 항목으로 두지 못했다.
- 응급 조치로 `workflow_dispatch --ref devel` 를 실행해 병합된 로직을 돌렸다
  ([run 31137319200](https://github.com/edwardkim/rhwp/actions/runs/31137319200)). 고아 23건 2.50GiB 를
  삭제하고 **51개 9.07GiB → 28개 6.57GiB**, 십진 한도 대비 97.4% → 65.7% 로 내려갔다. 삭제 실패 0건,
  열린 PR 13건의 캐시는 모두 보존됐다. 문서에 남긴 관찰 기준치 6.57GiB 와 정확히 일치했다.
- `main` 반영은 메인테이너가 직접 처리하기로 했다. 우리 쪽 작업 없음.
- `devel` KEEP=1 재검토 근거로 캐시 접근 시각 분석을 이슈에 남겼다 — 다세대 쌍 8개 전수에서
  "신세대 생성 이후 구세대 접근" 0건이며, restore-keys 프리픽스 매칭이 항상 최신을 고르는 설계의
  귀결이다. 추가 절감 여지는 1.99GiB 이나 고아 정리 실측이 안정된 뒤로 미룬다.

상세 근거: [#4080 접근 시각 분석](https://github.com/edwardkim/rhwp/issues/4080#issuecomment-5210357199),
[스케줄 트리거 문제](https://github.com/edwardkim/rhwp/issues/4080#issuecomment-5210362162),
[실측과 반영 요청](https://github.com/edwardkim/rhwp/issues/4080#issuecomment-5210827935).

## #4040 - 파일 게이트된 native-skia test 3건이 CI 에서 실행되지 않았다

- 이슈는 `issue_2293_chart_png_text.rs` 한 건만 지목했으나, 저장소를 훑어 확인하니
  `issue_2083_hide_fill_page_background.rs`·`issue_2292_chart_png_clip.rs` 도 같은 상태였다. 회귀 가드
  3건이 죽어 있었다. 이슈 본문을 3건 범위로 갱신하고 발견 경위를 코멘트로 남겼다.
- 결함이 셋이었다. ① Native Skia job 이 세 target 을 실행하지 않음 ② classifier 가 이 파일들을
  native-skia 소유로 보지 않아 `native_skia_required=false` 판정 — job 배선만 고치면 정작 그 파일을
  고치는 PR 에서 job 이 skip 된다 ③ 기존 계약 테스트가 job 이 실행하는 target 만 순회해 **양쪽 어디에도
  없는 파일**을 못 잡았다.
- workflow 에 `--test` 3개를 release-test·release 두 경로에 추가하고, classifier 소유 목록에 세 경로를
  더했다. 계약 테스트는 저장소를 훑어 부류 자체를 강제하도록 3건 추가했고 기존 단방향 테스트는
  역방향 감시용으로 남겼다.
- 발견 패턴은 중첩 게이트를 처리해야 한다 — `render_p37_direct_pdf_export` 가
  `#![cfg(all(not(target_arch = "wasm32"), feature = "native-skia"))]` 형태다. RED 재현에서 드러나
  판별식을 고쳤다.
- 로컬에서 세 테스트를 `native-skia` 로 직접 실행해 모두 통과했다(각 1 passed). 죽은 테스트를 되살린
  것이 아니라 유효한 검증이 CI 에서 빠져 있던 것이다.
- 계약 테스트 57건, node classifier 28건, `actionlint`, `node --check`, `git diff --check` 통과.
  뮤테이션 4종이 각각 해당 테스트를 실패시키는 것을 확인했다.
- PR 생성과 원격 CI 의 Native Skia job 로그 확인, 소요시간 증가폭 실측이 남았다.

상세 근거: [#4040 수행계획서](../plans/task_m100_4040.md),
[구현계획서](../plans/task_m100_4040_impl.md),
[#4040 Stage 1](../working/task_m100_4040_stage1.md).

## #4132 - 함수 단위 native-skia 게이트 test (신규 등록)

- `tests/cli_exit_codes.rs` 의 `export_png_follows_the_same_contract` 가 함수 단위
  `#[cfg(feature = "native-skia")]` 라 default 실행에서 cfg-out 되고 Native job 에도 없다.
- #4040 의 파일 게이트와 증상은 같지만 해법이 다르다. 이 파일은 `#[test]` 11건 중 1건만 게이트라,
  Native job 에 추가하면 나머지 10건이 중복 실행된다. 선택지 3안과 비용을 이슈에 정리했다.
- 다만 #4040 구현 중 `issue_2225_missing_picture_placeholder.rs` 가 `#[test]` 2건 중 1건만 게이트인
  채로 이미 Native job 에 등재돼 있음을 확인했다. 비게이트 1건이 이미 중복 실행 중이므로 선택지 A 는
  미지수가 아니라 선례가 있는 안이다. 이슈에 정정 코멘트를 남긴다.
- #4040 이 파일 게이트 규약과 계약 테스트 뼈대를 세우므로 그 뒤에 착수한다.

상세 근거: [#4132](https://github.com/edwardkim/rhwp/issues/4132).

## PR #4174 - #4159 종료 재귀 중첩 표 bottom clip

- [PR #4174](https://github.com/edwardkim/rhwp/pull/4174)를 `devel` 대상 Open PR로 생성했다.
  최신 `upstream/devel` `06f8ebcca`에서 `task_m100_4159_nested_table_bottom_clip` 브랜치를
  별도로 만들었다.
- 실제 fixture 물리 3쪽의 bottom 선은 생성돼 있으나 조상 분할 셀 clip보다 약 1.89px 아래에
  있어 SVG·Canvas2D에서 잘린다는 이슈 분석을 최신 코드에서도 재확인했다.
- 종료 셀의 재귀 자손만 bbox/clip에 포섭하고 비종료 조각은 현행 clip을 보존하는 구조 래칫부터
  작성했다. 집중 검증, 전체 PR 게이트, 원격 push·PR 생성은 각각 승인 뒤 순차 수행했다.
- 실제 구조 red는 `line_bottom=827.273 > clip_bottom=824.880`으로 재현했다. terminal
  clip 셀만 재귀 Table 자손 하단까지 확장한 뒤 release-test fixture 6개, Native Skia unit 2개,
  Clippy·fmt·TypeScript, release WASM, 신규 #4159와 인접 #536 Canvas2D E2E가 통과했다.
  새 SVG clip 하단은 stroke 하단과 같은 `827.273px`이며 한컴 기준과 시각 대조했다.
- 작업지시자가 rhwp-studio에서 물리 2쪽 사각형 숫자 출력과 물리 3쪽 표 하단 테두리를 확인해
  #4159 시각 판정을 통과시켰다.
- 별도 승인 뒤 전체 PR 로컬 게이트를 실행했다. release library 3,292건, 전체
  `release-test --tests`, Native Skia 58+2+4건, 전체 타깃 Clippy, doc-test, Studio 전체 802건,
  새 WASM 및 #4159·#536 E2E, manifest 86/86이 모두 통과했다.
- PR 생성 승인 뒤 최신 `upstream/devel` `23ff5b6f1`을 merge commit `e6f09003b`으로 통합했다.
  새 code head에서 전체 게이트를 다시 실행해 release library 3,305건, 전체 통합·Native Skia,
  Clippy·doc-test·Studio 802건, 새 WASM과 #4159·#536 E2E, manifest 86/86 통과를 재확인했다.

상세 근거: [PR #4174 검토](../pr/archives/pr_4174_review.md),
[#4159 수행계획](../plans/task_m100_4159.md), [#4159 Stage 1](../working/task_m100_4159_stage1.md).
