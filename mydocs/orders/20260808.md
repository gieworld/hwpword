# 오늘 할일 - 2026년 8월 8일

## 공통 — 운영 작업

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #4040 | PR #4170 변경 요청 2건 보정 | 진행중 | 관련 63+28건 통과, 보정 push·완료 코멘트 승인 |

## PR #4263 - HWPX BinData 압축 해제 상한 우회 보정

- contributor 원 head `6e5ee415679a440ca0ba59ff4b594a6ed2feab16`에서 HWPX lazy BinData가
  `load_limited(256MB)` 실패 뒤 HWP 저장 fallback 또는 `is_empty()` 질의로 무제한 압축 해제에
  도달할 수 있음을 확인했다.
- 메인터너 보정 `84e378e82`는 HWPX raw 표현이 없는 경우 빈 placeholder로 안전하게 접고,
  HWPX resolver의 길이·빈 상태 질의를 ZIP entry 크기 상한으로 변경했다. 실제 ZIP HWPX bomb의
  query, HWPX -> HWP, HWPX -> HWPX 회귀 6건을 추가했다.
- 최신 `devel` 충돌은 merge commit `c4bfc73e2`에서 양쪽 `layout.rs` import를 보존해 해소했다.
  최종 head의 fmt·diff check·Clippy·전체 `release-test --tests`는 통과했다.
- 원격 source branch push와 GitHub CI는 사용자 승인을 기다린다. 상세 근거는
  [PR #4263 검토](../pr/archives/pr_4263_review.md),
  [메인터너 보정 기록](../pr/archives/pr_4263_review_impl.md)에 남겼다.

## PR #4265 - humdrum00001010 표 편집·렌더 성능 통합

- 최신 `upstream/devel` `e919655a` 위 `review/humdrum00001010-20260808`에 @humdrum00001010의
  #4246, #4247, #4248, #4249, #4250, #4251, #4258, #4259, #4260, #4261, #4262를 누적했다.
  #4248/#4249의 단독 PR 충돌은 같은 성능 stack의 순서 의존성으로, 통합 PR에서는 두 변경을 함께
  적용해 해소한다.
- 통합 검증에서 중첩 표 border clip 때문에 #4248 fast path의 `cellBounds`가 legacy보다 좁아지는 KTX
  형상을 발견했다. 메인터너 보정 `da2d9ae75`는 대상 셀의 필요한 후속 문단만 compose하며 기존 giant-cell
  window를 유지한다. KTX parity와 giant-cell latency가 함께 통과했다.
- 전체 `release-test --tests`, focused Rust, Studio focused 27건·전체 813건, production build, WASM build,
  실제 HWP/HWPX 셀 Enter Chromium E2E를 통과했다. 로컬 Native Skia 3종은 준비 우선 지시로 중단했지만,
  `557bb8526`의 GitHub Full CI Native Skia job은 성공했다.
- [통합 PR #4265](https://github.com/edwardkim/rhwp/pull/4265)의 [CI](https://github.com/edwardkim/rhwp/actions/runs/31256100624),
  [CodeQL](https://github.com/edwardkim/rhwp/actions/runs/31256100547),
  [Render Diff](https://github.com/edwardkim/rhwp/actions/runs/31256100556)가 성공했다. 이 결과 기록
  문서 commit의 최신 docs-only gate와 mergeability를 확인한 뒤 작업지시자 승인에 따라 merge하면, 원 PR
  11개와 closing keyword 이슈를 후속 처리한다.

상세 근거: [통합 검토](../pr/archives/pr_4265_review.md),
[통합·메인터너 보정 기록](../pr/archives/pr_4265_review_impl.md).

## PR #4253 - kevin9327 연작 27건 통합 및 계약 보정

- [PR #4253](https://github.com/edwardkim/rhwp/pull/4253)는 #4186~#4244 중 기여자가 CI workflow를
  추가한 #4185를 제외한 27개 PR을 최신 `upstream/devel` 위에서 누적 통합한다. 원 기여 변경에는
  `.github/`가 없으며, 지식지도 출력 필드·Node 생성 봉투 타입·agent bench 태스크 ID의 메인터너
  보정이 포함된다.
- code candidate `cbfb07de3`에서 로컬 `release-test` 전체, Clippy, Python 267건, Node 420건과
  생성 타입·실제 CLI/도구 실행을 통과했다. 같은 head의
  [Full CI](https://github.com/edwardkim/rhwp/actions/runs/31248491466),
  [CodeQL](https://github.com/edwardkim/rhwp/actions/runs/31248491386),
  [Render Diff](https://github.com/edwardkim/rhwp/actions/runs/31248491377), Node/Python binding도
  모두 성공했고 PR 상태는 `CLEAN`이었다.
- [이슈 #4254](https://github.com/edwardkim/rhwp/issues/4254)로 분리한 binding trailing 문서
  fast-pass를 `374e14f72`에서 보정했다. 이 workflow 변경 자체는 fail-closed full lane으로 실행해
  [CI](https://github.com/edwardkim/rhwp/actions/runs/31249497089),
  [CodeQL](https://github.com/edwardkim/rhwp/actions/runs/31249496988),
  [Render Diff](https://github.com/edwardkim/rhwp/actions/runs/31249496981),
  [Node binding](https://github.com/edwardkim/rhwp/actions/runs/31249496995),
  [Python binding](https://github.com/edwardkim/rhwp/actions/runs/31249497087)을 모두 통과했다.
  이어 `mydocs/**`만 바꾼 `2fdf5c66d`에서
  [Node binding](https://github.com/edwardkim/rhwp/actions/runs/31250118443)과
  [Python binding](https://github.com/edwardkim/rhwp/actions/runs/31250118444)이 성공했고, 각
  unit·integration·package/wheel·generated-types job은 모두 skip됐다. 즉 코드 후보의 동일
  binding green 결과를 재사용하면서 branch protection의 최신 workflow 상태도 green으로 유지했다.
- 상세 수용 범위와 보정 근거는
  [통합 검토](../pr/archives/pr_4186_4244_kevin9327_series_review.md)와
  [구현 기록](../pr/archives/pr_4186_4244_kevin9327_series_review_impl.md)에 남겼다. 이 trailing
  문서 commit의 docs-only gate와 작업지시자 승인을 확인한 뒤 PR을 merge하고, 원 기여 PR 27건을
  통합 반영 사실과 함께 close한다.

## Issue #4252 - 재귀 분할 중첩 표 객체 선택 경로 복원

- `samples/basic/issue2007_nested_cell_pagination_42065.hwp`를 rhwp-studio에서 열고 페이지 경계를
  넘어 이어지는 자식 표 셀에서 `Esc`로 표 객체 선택을 시도하면
  `경로[1]: controls[0]가 표가 아닙니다` 오류가 발생한다.
- 실제 fixture 전 페이지의 `TextRun.cell_context.path`를 원본 IR에 재적용한 결과, 물리 2·4쪽의
  비분할 경로는 유효하지만 물리 5쪽부터 재귀 분할 조각이 `parentPara=0`, `control=0` 합성 좌표를
  노출한다. 실제 `paragraph[0].controls[0]`은 `SectionDef`라 선택 bbox 조회가 실패한다.
- 중복 검색에서 #2880은 복사·잘라내기 호출의 경로 전달 누락으로 별도 원인임을 확인하고, 결함을
  [#4252](https://github.com/edwardkim/rhwp/issues/4252)로 등록했다. assignee `edwardkim`, milestone
  `v1.0.0`, labels `bug`·`rhwp-studio`·`table`·`layout`을 지정했다.
- 최신 `upstream/devel` `fcc3b2135` 기준 `fix/issue-4252-nested-table-selection-path` 브랜치를 만들었다.
  수행·구현 계획 검토 뒤 renderer 경로 출처 보존과 실제 fixture red→green 래칫을 진행한다.
- 작업지시자는 이번 정정으로 사용자 브라우저 성능이 저하되지 않아야 한다고 지시했다. 새 전 페이지
  탐색·idle callback·커서 이벤트 작업을 추가하지 않고, 기존 조판 시점의 context 전달만 정정한다.
  수정 전후 동일 WASM에서 문서 로드와 자식 표 객체 선택 시간을 계측하고 기존 입력 성능 probe도 확인한다.
- 승인된 계획대로 재귀 부분 표가 enclosing `CellContext`를 보존하도록 수정했다. 실제 fixture의 잘못된
  중첩 경로 245건은 0건이 되었고, 물리 5쪽 `Esc` 자식 표 선택·외곽선·핸들이 새 WASM에서 통과했다.
- 작업지시자의 재검증 로그에서 hit-test 경로 181건, 표-only 부모 caret, 중복 선택 렌더와 비캐시 전
  페이지 재조판을 추가로 확인했다. hit-test 오류는 0건, `Esc` 선택 렌더는 2회 → 1회,
  물리 5쪽 bbox lookup은 경로 정정 직후 26.6ms → 최종 0.4ms로 정정했다. 부모 caret도 실제 경로로
  복원되며 관련 Studio 경고는 0건이다.
- 최종 WASM SHA-256은 `21663c57767b3bca3a5ac53598568fa7f12184b7f20df1adb86114d700c25225`다.
  기존 물리 2쪽 bbox lookup은 5.5ms → 0.1ms, 선택 renderer는 35.7ms → 1.2ms였고,
  #3137 입력 probe도 operation p95 1.90ms·long task 0·동기 pagination flush 0으로 통과했다.
- `requestIdleCallback`·`requestAnimationFrame` 장시간 경고는 변경하지 않은 기존 페이지 prefetch와
  visible-page 렌더 경로다. #4252의 키 입력 hot path와 분리하며, 계속 재현되면 별도 성능 이슈로
  추적한다.
- 작업지시자가 최종 WASM을 적용한 rhwp-studio에서 동일 자식 표 선택·커서 동작을 재검증하고 시각
  판정을 통과시켰다.
- PR 후보 준비 승인에 따라 Stage 1 commit을 최신 `upstream/devel` `e919655a7` 위로 재배치했다.
  138개 후속 commit 중 #3820·#2007 renderer 구조 변경에 맞춰 enclosing cell context를 새
  `layout_partial_table_resolved()` 경계까지 전달했다.
- release build·library 3,330건·전체 release-test integration·Native Skia 58+2+4건·Clippy·fmt·
  diff·rustdoc, Studio 타입 검사와 sandbox 밖 단위 테스트 802건을 통과했다. sandbox 안의 최초
  Studio 실행에서 자식 Node `spawnSync`가 `EPERM`으로 차단된 5건은 동일 전체 명령을 sandbox 밖에서
  재실행해 환경 제한임을 확정했다.
- 최신 Docker WASM SHA-256은
  `23eeecee687a695427106415787a876ef88126f948519a0ca93f03d6ef0ff564`이며 실행 중인 dev 서버의 제공
  해시와 일치한다. #4252 선택 E2E는 경고 0·렌더 1회, #4159는 17쪽·종료선 1,309/1,316 픽셀,
  #3137 입력 probe는 operation p95 2.00ms·full repaint 0·long task 0으로 통과했다.
- Stage 1 시각 판정은 이전 WASM 해시 기준이므로 최신 재배치 바이너리를 별도로 확인했다.
  작업지시자가 dev 서버에서 #4252 자식 표 선택·부모 caret 동작의 시각 판정을 통과시켰다.
  로컬 PR 후보 검증은 완료됐으며 원격 push·PR 생성은 별도 승인 대기다.
- 원격 게시 승인 직전 `upstream/devel`이 `59b31e5ce`까지 다시 30개 commit 전진해 #4252의 로컬
  3개 commit을 두 번째로 rebase했다. #4248 cursor probe의 새 부분 표 호출에는 최상위 enclosing
  context `None`을 전달해 최신 함수 계약과 통합했다.
- 최종 기준에서 release library 3,348건·전체 release-test integration·Native Skia 58+2+4건·
  Clippy·rustdoc, Studio 타입 검사·sandbox 밖 단위 테스트 813건과 E2E manifest 90/90을 통과했다.
- 새 Docker WASM은 7,701,996 bytes, SHA-256
  `328457d78e7af88ced916a63811f8ad76cca12c6762777ac6aca6f69d3dadd5b`이며 dev 서버 제공 해시와
  일치한다. #4252는 경고 0·렌더 1회·물리 5쪽 bbox 0.4ms, #4159는 종료선 1,309/1,316 픽셀,
  #3137은 operation p95 1.80ms·full repaint/long task/flush 0으로 통과했다.
- 앞선 수동 시각 판정은 이전 WASM 해시 대상이므로 최종 `328457d7…` 바이너리의 시각 재확인 뒤
  승인된 원격 push·Open PR 생성을 진행한다.
- 작업지시자가 최종 `328457d7…` WASM에서 #4252 자식 표 선택·부모 caret 복귀의 시각 판정을
  통과시켰다. 기존 게시 승인에 따라 branch push와 `devel` 대상 Open PR 생성을 진행한다.
- 코드 후보 `bd51c4796`을 [PR #4267](https://github.com/edwardkim/rhwp/pull/4267)로 게시했다.
  Open·non-draft이며 reviewer `jangster77`, assignee `edwardkim`, milestone `v1.0.0`, labels
  `bug`·`rhwp-studio`·`layout`·`table`로 접수했다.
- 작업지시자의 최종 시각 판정 근거를
  [PR 보존 자산](../pr/assets/pr_4267_page5_child_table_object_selection.png)으로 복사하고
  [PR 검토 기록](../pr/archives/pr_4267_review.md)에 로컬 검증, 대형 PR 판단, 성능 위험과 남은
  required checks를 기록했다. 코드 후보 CI는 일부 성공·일부 진행 중이므로 최신 trailing head의
  Actions 성공과 reviewer 검토, 명시적 merge 승인 뒤 병합한다.

상세 근거: [수행계획](../plans/task_m100_4252.md),
[구현계획](../plans/task_m100_4252_impl.md),
[Stage 1 보고서](../working/task_m100_4252_stage1.md),
[Stage 2 보고서](../working/task_m100_4252_stage2.md),
[Stage 3 보고서](../working/task_m100_4252_stage3.md).

## PR #4165 - 한글 버전 오라클 r1 정합 검증 보정

- contributor 원 source head `39c535583`을 최신 `upstream/devel` `d9c530ee8`과 대조했다. 충돌은
  `mydocs/orders/20260807.md` 하나였고 PR 고유 변경이 없는 과거 오늘할일이므로 최신 기준을 보존해
  merge tree를 clean하게 만들었다.
- r1 검증기가 최신 보고서의 최초 실행 ERR 열과 `<code>` 문서 경로를 처리하지 못하던 문제를 보정했다.
  41개 정합 검사는 `41 PASS · 0 FAIL · 0 CONTRADICTION`, 오라클 단위 3건, 링크 단위 5건, scripts
  발견형 118건은 모두 통과했다.
- PR 표기상 대형 누적 diff와 달리 최신 기준의 실제 변경은 Python·보고서·링크 검사 6파일이다. Rust,
  renderer, fixture, PowerShell 변경이 없어 Cargo/Native Skia/Windows COM 재실행은 하지 않았다.
- 작업지시자의 push 승인 뒤 contributor branch에 보정과 이 검토 기록을 반영하고, 최신 head Full CI와
  CodeQL 성공 및 mergeability 재확인 뒤 별도 merge 승인에 따라 처리한다.

상세 근거: [PR #4165 검토](../pr/archives/pr_4165_review.md).

## PR #4174 - #4159 종료 재귀 중첩 표 bottom clip 재검토

- code candidate `5ebeb5d8813c89d3d057be354bdd311e18e87656`의 GitHub Full CI, CodeQL, Render
  Diff 성공을 확인했고, 이번 재검토에서는 source·test·fixture·workflow 보정을 추가하지 않았다.
- 최신 `upstream/devel` `94fbcbedc`을 source에 반영한 current-base merge tree를 확인했다. 수동
  conflict 해소가 없고 `git diff --check`가 통과했다.
- focused Rust fixture 6건, host fallback WASM build, Canvas2D E2E를 실행했다. E2E는 17쪽과 물리
  3쪽 종료선 1,196 / 1,203 잉크 픽셀을 확인했다. Docker가 설치되어 있지 않아 표준 Docker WASM
  검증은 실행하지 못했고 host fallback임을 검토 기록에 명시했다.
- 재검토 초기에 시작한 로컬 `release-test` 전체는 exact code candidate의 GitHub Full CI 성공과 코드
  보정 부재를 확인한 뒤 중지했으며 완료 검증으로 기록하지 않았다. Native Skia 광범위 재실행도
  중복이므로 생략했다. 이 판단 조건과 기록 의무를 PR 검토 워크플로우·로컬 검증 가이드에 추가했다.
- 이 review·오늘할일 commit을 push한 최신 head의 fast-pass 또는 fallback aggregate가 성공하고
  mergeability를 재확인한 뒤, 작업지시자 승인에 따라 merge와 #4159 후속 처리를 진행한다.

상세 근거: [PR #4174 검토](../pr/archives/pr_4174_review.md).

## PR #4227 - #4158 실제 CharOverlap 사각 숫자 tofu 수정

- 원격에 게시하지 않았던 기존 `task_m100_4158_char_overlap_boxed_pua` 구현을 확인하고 최신
  `upstream/devel` `5a4f26d0d`을 merge commit `5356207db`으로 통합했다.
- 과거 오늘할일과 E2E manifest 충돌은 #4158 기록과 이미 병합된 #4159 기록을 모두 보존해 해소했다.
  현재 `devel`에는 #4158의 renderer 의미 헬퍼와 Canvas2D 래칫이 별도로 들어오지 않았음을 재확인했다.
- 현재 head에서 focused Rust 3건, Native Skia 2건, release WASM, #4158 물리 10쪽 7개 계약,
  인접 #536 물리 2쪽 6개 계약과 #4159 물리 3쪽 2개 계약, E2E manifest 87/87, fmt·diff 검사를
  통과했다. 새 crop은 `공정거래위원회` 앞 표식을 tofu가 아닌 사각형 안 숫자 1로 출력한다.
- Open PR #4227을 `devel` 대상으로 생성했다. assignee `edwardkim`, reviewer `jangster77`, milestone
  `v1.0.0`, labels `bug`·`rendering`을 지정했다. PR #4228은 이 branch 위의 후속 stack이다.
- 작업지시자가 물리 10쪽 사각 숫자의 rhwp-studio 시각 판정을 통과시켰다. 같은 과정에서 발견된
  일반 `TextRun` `U+F02FB` tofu는 별도 PUA 표시 작업으로 분리한다.

상세 근거: [PR #4227 검토](../pr/archives/pr_4227_review.md),
[#4158 Stage 1](../working/task_m100_4158_stage1.md),
[#4158 보고서](../report/task_m100_4158_report.md).

## PR #4228 - #4224 `U+F02FB` 작은 오른쪽 방향 삼각형 PUA

- `samples/basic/pau-004.hwp`에서 일반 `TextRun`의 첫 문자가 Supplementary PUA-A
  `U+F02FB`임을 확인했다. 한컴 문자표와 `함초롬돋움` 실제 glyph에서 작은 오른쪽 방향
  삼각형이며, 작업지시자가 같은 의미를 확인했다.
- #4158의 실제 `CharOverlap` 사각 숫자와 의미상 독립된 결함이지만, 통합 WASM에서 두 동작을 함께
  검증하도록 #4158 head `27932685b` 위에 `task_m100_4224_pua_f02fb_small_triangle`을 stacked branch로
  배치했다.
- IR 원문은 보존하고 paint 표면만 표준 `U+25B8` `▸`로 투영한다. 실제 샘플 Rust·SVG 래칫과
  WASM Canvas2D 증적을 남긴 뒤 전체 게이트·원격 단계는 별도 승인받는다.
- 실제 샘플 RED 2건을 확인한 뒤 검증 표 한 항목으로 GREEN 전환했다. 독립 branch 기준 Rust·SVG,
  Native Skia, 인접 PUA 13건, Clippy·fmt·diff, release WASM, Canvas2D 6개 계약,
  manifest 87/87과 두 backend PNG 출력이 통과했다. 재배치된 구현은 `3f0974dc8`에 기록했고 시각 증적은
  `output/pau-004/`에 보존했다.
- 독립 branch WASM에서는 #4158 사각 번호가 빠진 것을 확인했다. 렌더러 충돌이 아니라 branch 구성
  누락이므로 삼각형 branch를 #4158 head 위로 재배치했다. 결합 head에서 네이티브 `rhwp`와 release
  WASM을 새로 만들고 #4158 사각 번호 7개, 삼각형 6개 Canvas2D 계약과 manifest 88/88을 통과했다.
- 작업지시자가 결합 WASM의 사각 번호와 작은 오른쪽 방향 삼각형을 rhwp-studio에서 확인하고 시각
  판정을 통과시켰다.
- 전체 PR 게이트 승인을 받아 stacked 후보 `5f6569062`를 검증했다. IR field sweep 815개 샘플과
  112,314건은 기존 baseline과 같았고, overflow-cell은 감소·제거된 값만 엄격하게 낮춘 뒤 676개
  샘플·1,849줄과 일치했다. release library 3,307건, 전체 release-test integration suite,
  Native Skia 58+2+4건, Clippy·fmt·diff·rustdoc을 모두 통과했다.
- Studio 타입 검사와 전체 단위 테스트 802건, release WASM, E2E manifest 88/88, #4158 7개 계약,
  삼각형 6개 계약을 통과했다. OVR5는 `devel@5a4f26d0d` 대비 5문서·142쪽·11개체에서 geometry
  회귀 0건이다.
- 중복 검색 뒤 이 결함을 #4224로 등록하고 branch·task 문서·Rust/E2E 식별자를 정규화했다. Open
  stacked PR #4228을 #4227 branch 대상으로 생성했다. assignee `edwardkim`, reviewer `jangster77`,
  milestone `v1.0.0`, labels `bug`·`rendering`을 지정했다.
- #4227의 review-only head를 정확한 2-parent `mydocs/` 전용 merge bridge로 반영했다. #4227이
  `a377027ce`로 merge된 뒤 #4228 base를 `devel`로 retarget했다. `devel...#4228`은 13개 파일,
  +552/-10의 #4224 독립 diff만 남고 mergeable·`git diff --check`가 통과했다.
- retarget 상태를 검토 문서에 동기화하는 trailing commit으로 `synchronize` 이벤트를 만들고, 최신
  CI·CodeQL·Render Diff를 확인한 뒤 별도 merge 승인을 받는다.

상세 근거: [PR #4228 검토](../pr/archives/pr_4228_review.md),
[수행계획](../plans/task_m100_4224.md),
[Stage 1](../working/task_m100_4224_stage1.md).

## PR #4154 - tracked `output/poc` 정리 사후 보완

- 잊고 있던 self PR #4154를 수동 merge한 뒤 사후 감사했다. source head `445b6a936`의 CI run
  `31159210894`와 CodeQL run `31159210173`은 merge 전에 성공했고, merge commit
  `dc7d7adcc`의 첫 부모 대비 실제 변경은 `/output/` ignore 대상 POC 10개 삭제뿐이다.
- Draft 본문의 “참조된 5개 유지 또는 전체 삭제” 선택은 **전체 10개 삭제 유지**로 확정했다. 삭제
  파일은 복원하지 않고 task2004·task2019 문서에서 역사적·비추적 산출물임을 명시했다. 현행 장기
  근거는 tracked fixture·회귀 테스트·최종 보고서로 연결했다.
- Ready 전환 7초 뒤 reviewer·review 기록 없이 merge했고 assignee·label·milestone도 merge 뒤에
  설정된 절차 누락을 기록했다. 이 사례를 예외로 일반화하지 않고 이후 collaborator self PR에는
  reviewer 지정, review·오늘할일 포함, 최신 head 게이트와 merge 승인을 순서대로 적용한다.
- 완료된 원 PR의 기록만 담는 `mydocs/` 전용 후속 변경을 Open PR #4240으로 생성했다. assignee
  `edwardkim`, reviewer `jangster77`, milestone `v1.0.0`, label `documentation`을 지정했다.
- candidate `ac8bdbf61c2b760c8560d59d2f4d6a733ed6b823`의 review-only B 경로 preflight와 최종
  `Build & Test` aggregate, CodeQL preflight가 성공했다. 최신 `upstream/devel`과의 merge
  simulation, `git diff --check`, 영향 문서 8개의 상대 링크 검사도 통과했다.
- 작업지시자가 #4240을 maintainer self-review 경로로 처리하고 merge할 것을 승인했다. 최초
  `jangster77` reviewer 요청은 철회하고, 작성자 자기 `APPROVE`를 허용하지 않는 GitHub 제약 때문에
  검토 결과는 `COMMENT` review로 게시한다.
- 이 결정 기록을 포함한 최신 head의 required checks와 self-review 게시를 확인한 뒤 merge한다.
  완료된 원 PR의 issue·comment와 오늘할일은 반복하지 않는다.

상세 근거: [PR #4154 사후 검토](../pr/archives/pr_4154_review.md),
[PR #4240 검토](../pr/archives/pr_4240_review.md).

## PR #4182 - 폴더 문서 대량 추출·변환 레시피

- [PR #4182](https://github.com/edwardkim/rhwp/pull/4182)의 contributor @kevin9327 원 head
  `a7c82ba34`는 폴더 단위 `batch info`, `batch export-text`, `batch extract-data`,
  `batch convert`를 오류 행 재시도와 건수 게이트까지 연결한 레시피 9를 추가했다.
- 관련 [이슈 #4110](https://github.com/edwardkim/rhwp/issues/4110)이 요구한 기존 레시피 색인 행이
  누락돼, 메인터너 보정 `8bb09b23c`에서 지식 지도 링크와 검토 기록을 함께 추가했다.
- batch input·NDJSON·인증·문서별 limit·산출 이름 충돌·MCP 제외·집계 종료 코드 계약과 sample 5개,
  상대 링크를 대조했다. 공백 검사, 대상 문서 링크 검사, current-base merge 계산이 통과했다.
- 최신 head의 [CI 31240478000](https://github.com/edwardkim/rhwp/actions/runs/31240478000)과
  [CodeQL 31240477873](https://github.com/edwardkim/rhwp/actions/runs/31240477873)은 review-only
  fast-pass로 성공했고 Rust·Skia·WASM heavy worker는 의도대로 skip됐다.
- 작업지시자 승인 뒤 admin merge했으며 merge commit은
  `17ebc92d514f8103a09fce6aa91b5a19dd418d9f`다. 이슈 #4110은 merge 직후 GitHub Actions가
  closing keyword를 확인해 자동 close했으며, 운영 기록 반영 뒤 maintainer comment만 남았다.

상세 근거: [PR #4182 검토](../pr/archives/pr_4182_review.md).

## PR #4183 - 배포 전 보안 점검 스윕 레시피

- [PR #4183](https://github.com/edwardkim/rhwp/pull/4183)의 contributor @kevin9327 원 head
  `1dc664c4c`는 송신 전 hidden-text·injection·unicode 검사, 개인정보 dry-run·마스킹,
  metadata sanitize와 재검사를 연결한 레시피 10을 추가했다.
- 원 변경의 최종 재스윕 명령은 개인정보와 hidden-text만 다시 검사해, 문서가 약속한 네 축 중
  injection·unicode가 빠져 있었다. 메인터너 보정 `d91ad8fae`에서 두 명령과 실측 판정 필드를
  추가하고 지식 지도에 레시피 10 색인을 반영했다.
- v0.8.2 release 바이너리와 가짜 주민번호·전화·이메일로 실제 파이프라인을 실행했다. 초안에서
  개인정보 3건, 처리 뒤 redact 0·hidden 0·injection 0·unicode 0, sanitize 10건 제거,
  `verify.identical=true`를 확인했다. 대상 문서 링크·공백 검사도 통과했다.
- 최신 head의 [CI 31241512361](https://github.com/edwardkim/rhwp/actions/runs/31241512361)과
  [CodeQL 31241512293](https://github.com/edwardkim/rhwp/actions/runs/31241512293)은 review-only
  fast-pass로 성공했고 Rust·Skia·WASM heavy worker는 의도대로 skip됐다.
- 작업지시자 승인 뒤 admin merge했으며 merge commit은
  `01f4360acd719657e692284f3d38a25af654359b`다. 이슈 #4111은 GitHub Actions가 closing
  keyword를 확인해 자동 close했으며 운영 기록 반영 뒤 maintainer comment만 남았다.

상세 근거: [PR #4183 검토](../pr/archives/pr_4183_review.md).

## PR #4184 - PII 읽기 전용 검사 발견 경로

- [PR #4184](https://github.com/edwardkim/rhwp/pull/4184)의 contributor @kevin9327 원 head
  `65db7bc08`은 별도 `inspect pii` 승격 대신 기존 `edit redact --dry-run --no-raw --json`을
  개인정보 읽기 전용 검사 경로로 지식 지도에 추가했다.
- CLI의 두 플래그와 달리 MCP `hwp_redact`의 `dryRun`·`noRaw`는 자동 기본값이 아니므로,
  메인터너 보정 `5d7c5ea9c`에서 `dryRun:true`, `noRaw:true`를 함께 지정하도록 명확히 했다.
- v0.8.2 MCP schema, #4112 재판정 댓글, #4183의 실제 dry-run 결과, 대상 링크, 공백 검사와
  current-base merge 계산을 대조했다. 문서 전용 변경이므로 Cargo·Skia·WASM·시각 검증은 생략했다.
- 최신 head의 [CI 31242060882](https://github.com/edwardkim/rhwp/actions/runs/31242060882)와
  [CodeQL 31242060805](https://github.com/edwardkim/rhwp/actions/runs/31242060805)는 review-only
  fast-pass로 성공했고 PR은 `mergeable=true`, `clean`이었다.
- 작업지시자 승인 뒤 admin merge했으며 merge commit은
  `9831159eeba63ff2d0661ef9ebf6ca23e07aabc6`이다. 이슈 #4112는 merge 직후 자동 종료됐다.

상세 근거: [PR #4184 검토](../pr/archives/pr_4184_review.md).

## PR #4093 - VS Code 개요 번호 탐색 패널

- 첫 기여자 @walnutkim의 변경은 VS Code 뷰어에 HWP/HWPX 개요 번호 계층을 표시하고, 항목 이동과
  키보드 접기·펼치기를 제공한다. 표 셀 번호 문단도 렌더러와 같은 순서로 번호 상태에 반영한다.
- 기존 변경 요청의 무관한 Python binding 변경과 빈 `closes #` 표기는 제거됐다. 표 셀 번호 카운터와
  접기 버튼 초점 문제는 전용 HWPX fixture, Rust·프런트엔드 테스트, 실제 production webview와
  release WASM 브라우저 검증으로 해소했다.
- code head의 Full CI·CodeQL·Render Diff와 최종 review-only head `179a5e69c`의 fast-pass가
  성공했다. 작업지시자는 `output/4093/`의 VS Code 증적을 확인해 시각 판정을 통과시켰다.
- 두 inline review thread를 resolve하고 최신 head에 maintainer approval을 게시했다. 최종 상태를
  재검증한 뒤 작업지시자 승인에 따라 admin merge했으며 merge commit은
  `c119e5db5829df2e27850258500e5e2dd74a0015`다. 종료할 관련 이슈는 없다.

상세 근거: [PR #4093 검토](../pr/archives/pr_4093_review.md).

## PR #4257 - #3820·#3821 중첩 표와 PDF fidelity 복원

- [PR #4257](https://github.com/edwardkim/rhwp/pull/4257)은 정책 연구 문서의 지연 Square
  그림·표·각주 page owner와 issue2007 대형 중첩 `RowBreak` 표 분할을 단계별로 보정한다.
  정책 문서는 한컴 2020 PDF와 `215/215`쪽, issue2007은 `17/17`쪽이다.
- code candidate `f82e03444`에서 focused 15+3+8건, release lib 3,322건, 전체
  release-test integration, overflow-cell 674개 샘플, Native Skia 58+2+4건, fmt·Clippy·
  rustdoc을 통과했다. Studio 802건, fresh WASM, 브라우저 E2E 3종과 manifest 88/88도
  통과했다.
- p7–p17 visual sweep은 requested/completed/missing `11/11/0`, 전체 SVG/render-tree
  `17/17`, 자동 구조 후보 0쪽이다. 전 페이지를 공식 PDF와 직접 대조했고 대표 asset과
  실행 provenance를 PR branch에 보존했다.
- #4253과 code candidate끼리의 공통 경로는 `mydocs/manual/cli_commands.md` 하나이고,
  이 trailing 오늘할일까지 포함한 최종 후보에서는 `mydocs/orders/20260808.md`가 추가된다.
  두 경로 모두 수정 hunk가 겹치지 않으며 #4253 병합 뒤 최신 `upstream/devel`과의 merge
  simulation도 통과했다. code candidate `f82e03444`의 Full CI·CodeQL·Render Diff가 성공해
  이 review-only 기록을 push하고 최신 fast-pass aggregate를 확인해 merge한다. #3821은 종료
  의도대로 후속 확인하고 #3820은 남은 별도 실문서 후보를 위해 유지한다.

상세 근거: [PR #4257 검토](../pr/archives/pr_4257_review.md),
[Stage 63 최종 게이트](../working/task_m100_3820_stage63_final_pr_gate.md).

## PR #4266 - #4150 IME 조합 오버레이 stale 좌표 + HF/FN 재정박 offset

- 로컬 `integration/all-works`(#4180/#4179 등 다른 이슈와 미커밋 무관 변경이 섞인 통합 branch)에서
  IME 조합 관련 커밋 2개(`e6a20f92d`, `c03ee47a0`)만 최신 `upstream/devel` 위 별도 worktree로
  cherry-pick해 분리했다. 원 branch를 그대로 push하면 185커밋 뒤처진 base와 무관한 이슈가 같은
  PR에 섞이기 때문이다.
- 1차 커밋은 `compositionAnchorRect` 캐시를 제거해 같은 페이지 안 reflow 뒤 조합 오버레이가 옛
  좌표에 겹쳐 그려지던 결함(#4150)을 고치고, 조합 replace가 wasm deferred replace 범위 가드에
  거부될 때 `onInput` 밖으로 예외가 던져져 조합 추적이 wedge되던 경로를 try/catch로 방어한다.
  검토 중 이 catch 재정박이 머리말/꼬리말·각주 모드에서 `cursor.getPosition()`의 stale 본문
  offset을 그대로 쓰는 회귀를 발견했고, 2차 커밋이 `onCompositionStart`와 동일한
  `hfCharOffset`/`fnCharOffset` override로 고쳤다 — mock `this`로 `onInput`을 직접 호출해 HF/FN/
  본문 3가지 모드의 재정박 offset을 검증하는 행위 테스트를 추가했다(수정 전 fail, 수정 후 pass
  확인).
- cherry-pick한 code head `c03ee47a0`에서 `npx tsc --noEmit`, `node --test`(805/805 pass),
  `git diff --check`를 통과했다. Rust/wasm 변경이 없어 기존 `pkg/`를 재사용했고 fresh
  `wasm-pack build`는 생략했다.
- `upstream`에는 직접 push 권한이 없어(403) fork(`origin`)로 push하고 `edwardkim/rhwp` devel 대상
  Open PR을 생성했다. 본문에 `Closes #4150`을 명시했으나 저장소 기본 branch가 `main`이라 devel
  merge로는 자동 종료를 보장하지 않는다. 최근 동일 작성자 PR과 같이 `jangster77` reviewer 지정을
  시도했으나 권한 부족으로 실패해 작업지시자가 수동 지정해야 한다.
- 최신 head의 required CI 성공과 reviewer 지정·승인 확인 뒤 merge를 진행한다.

상세 근거: [PR #4266 검토](../pr/archives/pr_4266_review.md).
