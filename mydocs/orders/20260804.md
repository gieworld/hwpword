# 오늘 할일 - 2026년 8월 4일

## #4030 - 각주에서 대형 문서 찾아가기 본문 전환

- 실제 219쪽 HWP의 각주 1번에서 `Option+G → 200`을 수행하면 본문 위치는 바뀌어도 각주 전용
  caret·상태 갱신이 결과를 덮어 `10 / 219 쪽`으로 남는 것을 재현했다.
- 페이지 화면 이동이 성공한 뒤에만 각주 모드를 종료하고 본문 cursor를 배치하도록 보정했다. 이동
  실패 경로는 기존 각주 편집 상태를 보존한다.
- 실제 fixture E2E에서 각주 모드 종료, `200 / 219 쪽`, viewport `225242 / 225377.5`, 문단 2191
  cursor, 다음 `Option+G` 재호출을 확인했다. TypeScript, E2E manifest 84/84, 기존 #3953/#4026
  E2E와 Studio 단위 테스트 763/763도 통과했다.
- [PR #4033](https://github.com/edwardkim/rhwp/pull/4033)을 `devel` 대상으로 생성했다. archive review와
  오늘 기록을 포함한 최신 head의 GitHub Actions 통과 및 작업지시자 승인을 확인한 뒤 병합한다.

상세 근거: [#4030 Stage 1](../working/task_m100_4030_stage1.md),
[PR #4033 검토](../pr/archives/pr_4033_review.md).

## #4026 - 각주 편집 전역 단축키

- 각주 편집 모드의 조기 반환 때문에 macOS `Option+G`와 `Cmd+Z`가 공통 단축키 라우팅에
  도달하지 못하는 문제를 보정했다. `undo`, `redo`, `goto`만 서브모드 공통 명령으로 먼저
  전달해 각주 입력·이동·삭제·Escape 동작은 바꾸지 않았다.
- 실제 `footnote-01.hwp`에서 각주 텍스트 입력 후 `Cmd+Z` 커서 복원과 `Option+G` 찾아가기
  대화상자 표시를 headless browser E2E로 확인했다. TypeScript, Studio 단위 테스트 763건,
  E2E MANIFEST 83개 파일/83개 행도 통과했다.
- [PR #4027](https://github.com/edwardkim/rhwp/pull/4027)을 `devel` 대상으로 생성했다. archive
  review와 오늘 기록을 포함한 최신 head의 GitHub Actions 통과 및 작업지시자 승인을 확인한 뒤
  병합한다.

상세 근거: [#4026 Stage 1](../working/task_m100_4026_stage1.md),
[PR #4027 검토](../pr/archives/pr_4027_review.md).

## #3953 - 대형 HWP 찾아가기 및 상태 표시줄 진입점

- `samples/정책연구용역사업 중간진도보고서(살아있는 간장 기증자의 의학적 선별기준 연구).hwp`의
  100쪽 이상 이동 실패를 보정했다. 표가 쪽 시작인 경우의 커서 위치 검증 실패를 인접 문단
  fallback으로 처리하고, 화면 이동은 커서 배치와 분리했다.
- 상태 표시줄의 `현재 쪽 / 전체 쪽`을 클릭해도 `Option+G`와 같은 `edit:goto` 대화상자를
  열도록 연결했다. 실제 219쪽 문서에서 158쪽 E2E와 직접 probe를 수행해 후반부 이동과
  모달 재호출을 확인했다.

상세 계획: [#3953 Stage 1](../working/task_m100_3953_stage1.md).

## PR #3949 - 중첩 표 부모 셀 anchor 상한

- [PR #3949](https://github.com/edwardkim/rhwp/pull/3949)를
  `review/planet6897-20260804`에서 최신 source head `99cd22d84`와 기준 `devel`
  `aeb5805cb`로 재검토했다. 작성자가 추가한 HWPX fixture와 한컴 2020 기준 PDF를 포함해
  수정 전후를 직접 비교했다.
- 수정 전 focused regression은 30쪽의 쪽 아래 overflow `1,293.6px`로 실패했고, 수정 후
  `951.1px`로 통과했다. 전체 `overflow_cell_baseline`도 `1 passed`(284.43초)로 완료했으며,
  `fmt`, Python 진단 도구 문법, diff whitespace를 확인했다.
- 기존 source head CI의 필수 검사는 모두 성공하고 병합 상태는 `CLEAN`/`MERGEABLE`이었다.
  archive 검토 기록과 한컴 2020 기준 PDF를 review-only commit으로 contributor source branch에
  push한다. 이후 최신 head의 fast-pass preflight와 `Build & Test` aggregate 성공, 병합 가능
  상태를 다시 확인한 뒤 사용자 승인 조건에 따라 merge 및 후속 정리를 수행한다.

상세 근거: [PR #3949 검토](../pr/archives/pr_3949_review.md),
[한컴 2020 기준 PDF](../pr/assets/pr_3949_planet6897_nested_table_hancom2020_reference.pdf).

## #3937 — 배분 정렬 browser glyph 폭 보정

- [이슈 #3937](https://github.com/edwardkim/rhwp/issues/3937)을 생성하고 postmelee에 할당했다.
- 최신 upstream/devel ec1b21096에서 Canvas2D/SVG의 배분 간격과 실제 glyph-fit 폭을
  분리한 stack 하단 branch를 재구성했다.
- spacing focused 42건, SVG 41건과 wasm32 library check를 통과했다.
- 최상단 production WASM의 HWP/HWPX 연속 입력은 줄 전환 11 / 69, 최종 숫자 73,
  최종 쪽수 116과 synchronous flush 0으로 모두 GREEN이었다.
- Draft PR [#3944](https://github.com/edwardkim/rhwp/pull/3944)를 GitHub Stack
  #3947의 첫 레이어로 게시했다.

## #3822 — prior-break 뒤 긴 token 반복 줄바꿈

- #3937 레이어 위에 #3822 composer 변경만 올린 두 번째 stack branch를 재구성했다.
- 최신 upstream/devel ec1b21096 기준 composer 52건과 #3822 전용 4건을 통과했다.
- 긴 token의 overflow 해결과 browser glyph 윤곽 확대의 책임을 각각 #3822와 #3937로
  문서에서 분리했다.
- 기존 HWP/HWPX 저장·재열기 증적은 역사적 결과로 보존하고, 최신 최상단 combined smoke에서
  두 형식 모두 줄 전환 11 / 69, 최종 숫자 73과 최종 쪽수 116을 다시 확인했다.
- Draft PR [#3945](https://github.com/edwardkim/rhwp/pull/3945)를 GitHub Stack
  #3947의 두 번째 레이어로 게시했다.

## #3815 — latest-revision pagination start coalescing

- #3822 위 최상단 branch에 scheduler 제품 변경과 실제 IME·반복 줄바꿈 E2E를 올렸다.
- 최신 upstream/devel ec1b21096 기준 focused 23건, TypeScript와 production WASM을
  다시 통과했다. Studio 전체 763건은 upstream에 Studio 파일 변경이 없어 기존 결과를 보존했다.
- HWP/HWPX continuous-only smoke는 wraps 11 / 69, digits 73으로 모두 GREEN이었다.
  pending operation p95 50.1ms / 50.9ms는 기존 smoke 대비 -0.2% / +2.4%이며,
  최종 쪽수 116, revision 132 / 132와 synchronous flush 0을 확인했다.
- Draft PR [#3946](https://github.com/edwardkim/rhwp/pull/3946)를 GitHub Stack
  #3947의 최상단 레이어로 게시했다.

## PR #3831 - Studio 표 나누기·붙이기 검토

- [PR #3831](https://github.com/edwardkim/rhwp/pull/3831)을
  `review/enigma-jerry72-20260804`에서 검토했다. 최신 `devel` `301d0fe5f`를 포함한 source
  head `97be5b362` 위에 메인터너 보정 `ab9baf359`을 재배치했다.
- 분할 전 손상 `row/span` 범위의 panic·wrap 위험과 저장 후 사라지는 local-resize hint에 따른
  표 폭 재계산 불일치를 보정했다. focused 2건, 전체 release-test, clippy, Native Skia, WASM,
  Studio TypeScript·759개 test·production build, headless browser 명령 E2E를 확인했다.
- 원격 PR CI는 현재 source head에서 모두 통과했다. 메인터너 보정은 아직 원격 branch에 push하지
  않았으므로, 사용자 승인 뒤 보정과 검토 기록을 push하고 CI 재통과를 확인한 뒤 merge한다.

상세 근거: [PR #3831 검토](../pr/archives/pr_3831_review.md),
[메인터너 보정 기록](../pr/archives/pr_3831_review_impl.md).

## Planet6897 PR #3900·#3924·#3926·#3936 누적 통합 검토

- 최신 `upstream/devel` `21affdafc` 위 기본 작업트리 branch
  `review/planet6897-20260804`에 열린 Planet6897 PR 4건을 번호순으로 `cherry-pick -x` 누적했다.
  원 contributor branch는 변경하지 않아 작성자와 source SHA provenance를 보존한다.
- 누적 검토에서 #3926 본문의 "host 줄 높이 gate 배제"와 달리 원 구현이 host 조건을 OR로 수용하는
  불일치를 발견했다. 메인터너 보정 `b2905829a`에서 다음 저장 사다리의 table·outer-margin 여유만
  허용하도록 좁혔고, synthetic line segment가 저장 위치 비교에 섞이지 않게 했다. #3936에는 미주 없는
  HWP3 body도 하나의 `ColumnDef`를 갖는 idempotence 회귀 `c6f2ddcb3`을 별도 추가했다.
- #3900 HWP는 한컴 2020 PDF 4/4쪽, #3926 HWPX는 전체 222쪽과 선택 23–24쪽을 대조했다. 자동 sweep의
  font metric false positive는 사람 검토로 배제했고 geometry·쪽 경계는 기준 PDF와 일치했다.
- focused 회귀, IR/overflow baseline exact match, 전체
  `cargo test --profile release-test --tests` 종료 코드 0, fmt, clippy, native-Skia PNG/PDF, WASM
  package build를 모두 확인했다. 원격 integration PR 생성·push와 CI·merge는 별도 사용자 승인 후에만
  진행한다.

상세 근거: [누적 구현·검토 기록](../pr/archives/pr_3900_3924_3926_3936_review_impl.md),
[#3900](../pr/archives/pr_3900_review.md), [#3924](../pr/archives/pr_3924_review.md),
[#3926](../pr/archives/pr_3926_review.md), [#3936](../pr/archives/pr_3936_review.md).

## Kevin PR #3916·#3920·#3921·#3922·#3923 누적 통합 검토

- 최신 `upstream/devel` `874dae394` 위 기본 작업트리 branch
  `review/kevin9327-20260804`에 열린 Kevin PR 5건을 번호순으로 `cherry-pick -x` 누적했다.
  #3916의 source `d0a1dc360`은 이미 devel에 반영된 #3884와 중복이라 제외하고, 실제 #3915
  보정 source `197391d9b`만 보존했다.
- 누적 검토에서 CI binary 경로·malformed HWP3 오류 oracle, `rhwp-agent chunk-plan`의 구조화 argv,
  빈 field expectation usage 오류, 새 매뉴얼 metadata를 메인터너 보정으로 고정했다.
- focused 21건, 실제 #3916 JSON 두 축 보고, 전체
  `cargo test --profile release-test --tests` 종료 코드 0, fmt, clippy, WASM library check를 확인했다.
  renderer/layout 변경은 없어 별도 PDF visual sweep 대상이 아니며, 전체 회귀의 visual baseline은 통과했다.
- 원 PR과 contributor branch는 변경하지 않았다. 이후 원격 통합 PR 생성·push와 CI·merge는 별도 사용자
  승인 후에만 진행한다.

상세 근거: [누적 구현·검토 기록](../pr/archives/pr_3916_3920_3921_3922_3923_review_impl.md),
[#3916](../pr/archives/pr_3916_review.md), [#3920](../pr/archives/pr_3920_review.md),
[#3921](../pr/archives/pr_3921_review.md), [#3922](../pr/archives/pr_3922_review.md),
[#3923](../pr/archives/pr_3923_review.md).

## PR #3840 - 차트 HWPX 구조 보존

- [PR #3840](https://github.com/edwardkim/rhwp/pull/3840)을 merge commit
  `78766a69636b7d9f5a4c93786817d80e61d9e5c4`로 `devel`에 반영했다.
- `Chart/` 파트 암호화·manifest·비밀번호 재열기 round-trip 회귀를 메인터너 테스트로 고정했다.
  CI의 고정 credential alert는 실행 process ID 기반 test 비밀번호로 보정했고 새 CodeQL aggregate가
  성공했다.
- 최신 head의 CodeQL, Render Diff, Lint, Native Skia, Build & Test가 성공했다. 관련 issue #3546은
  auto-close 상태를 확인하고, 원 PR과 issue에 merge·검증 결과를 기록한다.

상세 근거: [PR #3840 검토](../pr/archives/pr_3840_review.md),
[메인터너 보정 기록](../pr/archives/pr_3840_review_impl.md).

## Kevin PR #3808·#3886·#3887·#3889·#3897·#3898·#3899·#3903·#3908 누적 통합 검토

- 최신 `upstream/devel` `06cf03080` 위 기본 작업트리 branch
  `review/kevin9327-20260804`에 열린 Kevin PR 9건을 오래된 번호 순으로 `cherry-pick -x` 누적했다.
  원 contributor branch는 수정하지 않아 원 author와 source SHA provenance를 보존한다.
- 누적 검토에서 plan-schema가 비어 있는 operand를 허용하는 계약 불일치와 internal diagnostics
  command의 미지 flag 침묵 수용을 발견했다. 원 기여 commit과 분리한 메인터너 보정
  `fb33dfd72`, `45e53d42e`에서 이를 수정하고 focused regression을 추가했다.
- diagnostics focused 28건, plan-schema contract 26건, preflight 30 command, 전체
  `cargo test --profile release-test --tests` 종료 코드 0, fmt, clippy, doc test, WASM library
  check, 변경 Markdown 링크 검사를 성공으로 확인했다. 기본 WASM check의 native CLI binary 실패는
  기존 native-only API 경로이며 실제 `--lib` WASM 산출은 성공했다.
- renderer/layout 변경이 아니므로 별도 PDF visual sweep은 대상이 아니다. 전체 release-test의
  visual baseline은 통과했다.
- [통합 PR #3912](https://github.com/edwardkim/rhwp/pull/3912)를 생성했다. 최신 required CI와
  mergeability를 재확인하고, 사용자 승인 뒤에만 merge·원 PR close·review branch와
  `target/review-kevin9327-20260804` 정리를 진행한다.

상세 근거: [통합 구현·검토 기록](../pr/archives/pr_3808_3886_3887_3889_3897_3898_3899_3903_3908_review_impl.md),
[#3808](../pr/archives/pr_3808_review.md), [#3886](../pr/archives/pr_3886_review.md),
[#3887](../pr/archives/pr_3887_review.md), [#3889](../pr/archives/pr_3889_review.md),
[#3897](../pr/archives/pr_3897_review.md), [#3898](../pr/archives/pr_3898_review.md),
[#3899](../pr/archives/pr_3899_review.md), [#3903](../pr/archives/pr_3903_review.md),
[#3908](../pr/archives/pr_3908_review.md).

## M100 — v1.0.0 편집 기반

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #1528 | export-structure 후속 개선 상위 추적 | 진행중 | #3744 통합 뒤 최종 검증·종료 판단 |
| #3744 | clause 문맥 만료·날짜·목 confidence 보정 | 진행중 | Stage 4 전체 검증 완료, push·draft PR 생성 승인 |

## 세션 시작 점검 (메인테이너, 07:15 KST)

- devel 동기화(38커밋 유입), 작업트리 clean. 열린 PR 13건 / 이슈 88건.
- **보안 제보 대기**: [#3803](https://github.com/edwardkim/rhwp/issues/3803) 채널을
  어제 열었으나 **Advisory 접수는 아직 없음**. 작업지시자 판단으로 **하루 더 대기**한다.
  `mydocs/feedback/secu-002/` 로컬 자료는 계속 **미추적·미열람** 유지 — 정식 채널
  수신 전까지 손대지 않는다.
- **차트 편집 배관 진전**: [#3546](https://github.com/edwardkim/rhwp/issues/3546) 이
  PR #3840(JamesPsh) 으로 CLOSED. 어제 채택 결정한
  [#3683](https://github.com/edwardkim/rhwp/issues/3683)(차트 내용 편집) 의 **착수 순서
  1번이 완료**됐다. 다음은 [#3682](https://github.com/edwardkim/rhwp/issues/3682)
  Track A P0~P1(선택 = 편집 진입점).
- 어제 완결: #3664·#3890·#3891 FFI 3연쇄(크레이트 복구 → workspace 가드 확장 →
  래퍼 계약 가드), #3684 캐시 스윕, #3686 액션 SHA 고정, 열린 PR 35건 트리아지.

## PR 트리아지 (메인테이너)

열린 PR 13건 전건 메타데이터 설정 — 신규 유입 10건 처리(나머지 3건은 8/3 완료분).

- 규칙: **assignee = author**, **milestone = v1.0.0**, label 은 변경 파일 영역 +
  제목 접두어로 판별, **reviewers 미지정**.
- 분포: documentation 5 · cli 5 · bug 4 · enhancement 3 · 기타(api·ci·harness·
  layout·mcp·rhwp-studio·table·test 각 1).
- `gh pr edit` 는 Projects classic 사용중단 오류로 실패하므로 REST 로 처리한다
  (`gh api -X PATCH .../issues/{n} -F milestone=`, `.../labels`, `.../assignees`).

검토 대상 구조: kevin9327 PR 9건은 [#3912](https://github.com/edwardkim/rhwp/pull/3912)
통합 PR(jangster77)로 묶여 있어, 개별 검토 대상은 #3900(조판)·#3831(표 나누기/붙이기)·
#3823(draft) 3건이다.

## #3917 — CI main 반영 (jangster77 요청) 완결

- 요구사항: cherry-pick 통합 시 대체된 원 PR 의 CI·CodeQL 취소로 러너 슬롯 확보.
- [PR #3919](https://github.com/edwardkim/rhwp/pull/3919) merge — **트리 무관 8파일 축소안**
  (cancel-stale-pr-runs + SHA 고정 소수정 7). `Cancel stale PR runs` 워크플로가 main 에
  `active` 등록되어 **fork PR stale run 취소가 발동 가능** 상태가 됐다.
- 처음 계획(워크플로 13파일 전체)은 **4차례 CI 실패로 반증** — devel 샤드 CI 는 devel
  테스트 스위트·소스 기능과 한 몸(4차: `overflow_cell_baseline` 하드코딩, main 에선
  컴파일 불가). 샤드 재편은 다음 릴리스에서 소스와 함께 main 에 간다.
- 워크플로 파일 선택 규칙 재확인: `push`·`pull_request` 는 해당 브랜치 파일,
  `pull_request_target`·`schedule`·`workflow_dispatch` 는 default 브랜치 등록(#3508·#3684).
- 이슈 #3917 close, [콜레보레이터 회신](https://github.com/edwardkim/rhwp/issues/3917#issuecomment-5172983854)
  게시(운영 참고: 통합 후 즉시 close 하는 원 PR 은 synchronize 이벤트가 없어 수동 취소 필요).

## 공통 — 운영 작업

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #3790 | 모든 devel PR의 frontend-only 과잉 CI 조건화 | 진행중 | Stage 3 merge 및 canary PR #3951 완료(직접 runner time 7분 47초 절감). Stage 4 Rust·Native Skia 조건화 로컬 구현·집중 검증 완료, push·draft PR 승인 대기. cold release archive timeout은 #4029로 분리. controller는 정상 릴리즈 후 enforcement 근거로 보존 |

## #3674 완결 — 편람 쪽수 편차 정밀 진단 (3안)

- [PR #3934](https://github.com/edwardkim/rhwp/pull/3934) merge, #3674 CLOSED
  (close-issues-on-devel-push 정상 발동). **동작 무변경** — 진단 프로브+보고서만.
- 핵심: 편차 +3 은 상쇄 착시(실제 19쪽 규모), 게이트는 declared 0.1px 판정식,
  되감김 형상은 "행높이 측정 정합" 선결 발견으로 [#3931 이관](https://github.com/edwardkim/rhwp/issues/3931#issuecomment-5174790040).
- 서브 이슈 존속: #3927(B')·#3928(C)·#3929(A)·#3930(D)·#3931(E).
- 사고 1건 기록·메모리 등재: 백그라운드 트리 변형 중 git add -u 커밋 금지.

## #3681 완결 — 차트 C3 측정·분해 (A안)

- [PR #3941](https://github.com/edwardkim/rhwp/pull/3941) merge, #3681 CLOSED. 문서 전용.
- 28종 전수 베이스라인(중앙값 ink 20.49, **포맷 축 소거** hwp=hwpx 전종 동일),
  성분 분해: **1순위 프레임 상단 +57px 전종 공통(28/28)** → #3938,
  밴드 드리프트 8~180px → #3939, 3D·막대 스타일 → #3940 (전부 #3681 서브 연결).
- 기각: 쪽 수준 박스 기하·색상 팔레트. 목표선은 단일 % 대신 성분 수용 기준으로 치환.

## #3682 완결 — 차트 Track A 실측 (전제 반증)

- [PR #3948](https://github.com/edwardkim/rhwp/pull/3948) merge (CI 19 pass), #3682 CLOSED.
- **이슈 전제 반증**: "선택·이동·복사·삭제 전부 불가"였으나 실측상 대부분 이미 동작.
  진짜 갭은 **속성 다이얼로그 1건**이었고, 그것도 차트 전용이 아니라 **그림·도형 공통**
  배선 결함(커맨드 매핑 부재 + 개체 선택 분기가 키를 삼킴).
- 회귀 e2e 3종 FAIL 0, P6 저장 왕복 OLE 보존. 행동 실측 프로브 신설.
- [#3683 에 전제 재검토 코멘트](https://github.com/edwardkim/rhwp/issues/3683) — 선택
  진입점이 이미 있으므로 Track B 를 Track A 대기 없이 착수 가능.

## #3927 완결 — 편람 Q&A 잔여 발산 진단 (A안)

- [PR #3952](https://github.com/edwardkim/rhwp/pull/3952) merge (CI 18 pass), #3927 CLOSED.
  **동작 무변경** — 프로브 보강(sec 필드·mt_row_heights·DIAG_ADV)과 문서만.
- 근인: 구역 10 pi=23 표 **행4 줄당 높이 24.5px vs 한컴 18.8px**(1.30배). 줄 수·줄바꿈은
  정확(16줄 일치) — **160% 이중 적용 가설**.
- 진단 중 가설 2회 반증: ①"한컴이 분할" ✗ ②"선언값이 정답" ✗ →
  **저장값은 스냅샷, 정답지 PDF 실물 bbox 만이 오라클**(교훈 기록).
- 수정은 [#3931 이관](https://github.com/edwardkim/rhwp/issues/3931#issuecomment-5177423421)
  — 셀 줄높이는 전 문서 공통 경로, 10k 게이트 필요. 오늘 **두 번째로 "측정 정합이
  분할 교정의 선결"** 실증(#3674 issue_3738, #3927 편람).

## #3938 종결 — 차트 프레임 +57px 는 측정 아티팩트 (close)

- 실측: 잉크 세로 시작이 rhwp·한컴 **둘 다 y=132** — 배치는 정확. +57 은 하니스
  `detect_frame()` 이 옅은 rhwp 외곽선(밝기 241 vs 158)을 못 잡아 용지 여백선으로
  대체한 결과.
- 색차는 **rhwp/한컴이 서로 다른 차트 컴포넌트를 쓰는 데서 기인** — 정합 대상 아님
  (작업지시자 판단). #3938 not planned 로 close.
- **게이트 폐기 불필요**: 차트 테스트 6종 색 단언 0건, 골든 SVG 차트 0건,
  render-diff 차트 코퍼스 미포함 — 색 회귀 게이트가 애초에 없음.
- **#3939·#3940 지표 재정의**: 색 민감 `ink_match`(중앙값 20.49) → **색 무관 이진
  잉크 IoU**(중앙값 30.48). 하위군 13~15% vs 상위군 62~72% 로 기하 결함이 더 선명히
  갈린다. 28종 베이스라인을 각 이슈에 게시, "색차는 결함 아님" 명문화.

## 사고 대응 — 에이전트 오작동 대량 PR (kevin9327, 69건)

한 기여자의 **에이전트 오작동**으로 fork PR 69건이 하루에 생성돼 각각 CI·CodeQL·
Render Diff 를 돌렸다. 러너 포화(진행 15 + 대기 39)로 다른 작업이 대기 상태가 됐다.

### 조치

| 항목 | 결과 |
|---|---|
| 워크플로 취소 | **54건** — 열린 PR head 브랜치 목록과 대조해 devel CI 1건은 보존 |
| PR close | **69건** — **브랜치는 보존**(코드 남아 재오픈 가능), 타 기여자 1건 보존 |
| 잔여 액션 | 0건 |

1차 `gh run cancel` 후 **44건이 남아** `force-cancel` 2차가 필요했다(취소 반영 지연·
신규 큐잉).

### 커뮤니케이션 정정

close 코멘트에 "PR 규모를 조율해 달라"고 적었으나, 이후 **에이전트 버그**임을 전달받아
[대표 PR #4023 에 정정](https://github.com/edwardkim/rhwp/pull/4023#issuecomment-5179457422)
— 사과 + 코드 보존·재오픈 안내 + 선별 복구 제안. **원인 확인 전에 톤을 정한 것이
실책**이었다.

### 재발 대응 — 도구화 (#4025, PR #4028 merge)

- 방어 정책은 이미 있다: `approval_policy` 를 **릴리즈 때만** `all_external_contributors`
  로 올리고 평시는 `first_time_contributors` 유지(작업지시자 거버넌스). 정책 변경 없음.
- 평시 사고는 사후 정리로 대응 — `scripts/cancel_stale_pr_runs.sh --author <로그인>`
  추가. 열린 PR 브랜치 대조로 devel·main·타 기여자 보호, 2단 취소, dry-run.
- 구현 중 성능 결함 자체 검출: `--paginate` 전량 조회가 **180초 타임아웃** →
  status 필터로 **2.5초**. 사고 대응 도구가 느리면 사고 중에 못 쓴다.
- 매뉴얼에 **"정리 전 원인 확인"** 명문화(`multi_pr_update_branch.md`).

## devel 브랜치 보호 변경 — `strict: false` (update branch 강제 해제)

콜레보레이터(jangster77·postmelee)가 **update branch 없이 merge** 할 수 있도록
devel 의 `required_status_checks.strict` 를 **true → false** 로 변경했다.

### 왜 admin 부여가 아니라 이 방법인가

개인 계정 저장소에는 "merge 규칙만 면제"하는 중간 등급이 없다. admin 을 주면 브랜치
보호 변경·저장소 삭제·Secrets·Actions 설정까지 전부 따라온다. **권한 등급을 올리지
않고 목적만 달성**하는 경로가 `strict: false` 다.

### 변경 범위 (검증 완료)

| 설정 | 이전 → 현재 |
|---|---|
| devel `strict` | true → **false** |
| contexts(`Build & Test`) · enforce_admins · reviews · force_push · deletions | **전부 불변** |
| **main `strict`** | **true 유지**(불변) |

### 유지되는 안전장치와 남는 위험

- `Build & Test` 필수 체크는 그대로 — **CI 통과 없이는 merge 불가**.
- 다만 체크는 PR 브랜치 기준이고 **base 가 그 뒤 앞서갔는지는 보지 않는다** →
  PR CI 가 검증하지 않은 조합이 devel 에 들어갈 수 있다(같은 파일을 여러 PR 이
  건드릴 때 실제 가능 — 오늘 typeset.rs 축이 그런 사례).
- 완화: devel push 시 CI 가 다시 돌아 **사후 검출**은 된다. 충돌 가능성이 큰 PR 은
  merge 전 update branch 를 **권고**로 남긴다.

되돌리려면 `strict: true` 한 줄. 변경 전 전체 설정 백업은 세션 scratchpad 에 보관했다
(`devel_protection_backup.json`).
