# kevin9327 연작 통합 구현 기록 - #4186~#4244

## 누적 방식

1. `upstream/devel`의 `c119e5db5`에서 `review/kevin9327-20260808`을 만들었다.
2. #4186을 먼저 적용하고 #4192에는 #4186에 이미 포함된 커밋을 중복 적용하지 않았다.
3. source branch에 포함된 `Merge branch devel` 커밋은 적용하지 않았다.
4. #4194의 최종 재시도 커밋은 현재 기준선에서 빈 변경이라 생략했다.
5. #4233, #4239 충돌은 두 기능 축을 모두 보존하는 방식으로 해소했다.
6. 계약 시험이 발견한 세 보류 사유는 같은 가시성 브랜치에서 메인터너 커밋으로 보정했다.
7. #4185는 CI 워크플로 추가 PR이므로 작업지시자 지시에 따라 통합 커밋을 제거했다.

## 롤백 경계

- 원 PR별 기능은 모두 `cherry-pick -x` 출처를 보존한다.
- #4186/#4192는 하나의 `verify` 스택이므로 독립 롤백하지 않는다.
- #4239의 온톨로지와 Node 생성 타입 보정 `6e1686eb`은 함께 유지한다.
- 지식지도 보정 `fa274cb3`은 #4194/#4217/#4239 출력 계약을 함께 반영한다.
- CI를 바꾸는 #4185는 이 통합안의 롤백 대상이 아니라 처음부터 제외 대상이다.

## 원격 처리 전 조건

1. 각 원 PR의 head SHA, `maintainerCanModify`, mergeability와 required check를 다시 조회한다.
2. 원 PR head가 바뀌었으면 변경분만 같은 누적 브랜치에 재적용하고 해당 범위 검증을 다시 수행한다.
3. 원격 push, PR 생성, approve, merge, comment는 작업지시자 승인 뒤에만 수행한다.
