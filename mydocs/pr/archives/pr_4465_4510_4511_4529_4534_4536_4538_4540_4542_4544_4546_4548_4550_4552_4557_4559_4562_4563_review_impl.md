---
kind: review-implementation
status: local-ci-complete
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-11
---

# kevin9327 CI 성공 PR 18건 누적 검토 이행 기록

## 고정 기준

- local review branch: `review/kevin9327-20260811` (`/home/tsjang/rhwp` 기본 worktree에서 가시화)
- integration base: `upstream/devel` `c64b5c70a700850d9bc1290fb5d64ebae7145409`
- 현재 local candidate: `4d43457af` (최신 `upstream/devel` 위로 rebase한 선형 누적 head)
- 원 PR: #4465, #4510, #4511, #4529, #4534, #4536, #4538, #4540, #4542, #4544,
  #4546, #4548, #4550, #4552, #4557, #4559, #4562, #4563
- 적용 순서: 위 목록의 오래된 PR 번호순. 원 branch의 `Merge branch devel` commit은 누적 체리픽에서 제외했다.

## 완료한 누적과 보정

1. 각 원 PR의 기능·테스트·작업 기록 commit을 `-x`로 누적해 author provenance를 보존했다.
2. 최신 source head를 다시 fetch했다. #4544 이후 source branch의 후속 merge commit은 선행 변경을
   중복 포함했으며, 실제 fix commit은 현재 누적 후보에 patch-equivalent하여 cherry-pick이 빈 변경으로 확인됐다.
3. `75273093d`에서 생성 코덱스를 현재 CLI 83개와 정합화했다. `harness-status`는 읽기 전용 top-level
   명령으로 유지하고, `anchor`, `bundle`, `disclose`, `settle`의 하위 동작을 가짜 top-level 명령으로
   생성하지 않도록 했다.
4. #4562 충돌에서는 새 온보딩 지침과 기존 `llms.txt` roadmap 행을 모두 보존했다. stacked gym 제출
   산출물은 contributor cleanup 범위로 제외된 상태를 유지했다.
5. 최신 `upstream/devel`의 #4519 CodeQL 변경을 기준선으로 rebase했다. 기준선 전진분은 CI workflow, Python
   workflow 검증 도구와 문서만 변경하며 Rust·Node 구현은 바꾸지 않았다. 작업지시자 지시에 따라 rebase 뒤
   전체 회귀를 반복하지 않았다.
6. PR #4574의 생성 타입 최신 검사에서 capabilities 52개와 생성 파일 47개의 불일치를 발견했다.
   `bindings/node/src/envelopes.ts`를 현재 candidate 바이너리로 재생성해 audit-report, conformance,
   disclose, recall-scope, settle 타입을 추가했다.

## 완료한 검증

- `cargo fmt --all -- --check` 통과.
- `cargo build --bin rhwp --target-dir target/pr-review` 통과.
- `RHWP_BIN=/home/tsjang/rhwp/target/pr-review/debug/rhwp python3 tools/gen_agent_codex.py --check`:
  명령 83개, 실측 표본 18개, 변경 0으로 통과.
- signing, harness, anchor, gate, bundle, disclose, settle, audit, provenance, agent-codex, skills focused nextest:
  37 passed.
- `cargo nextest run --cargo-profile release-test --target-dir target/pr-review --tests --test-threads 12 --no-fail-fast`:
  **5,757 passed, 36 skipped, 7 slow, 448.545초**.
- Node 생성 타입 보정 뒤 `RHWP_BIN=target/pr-review/debug/rhwp npm run gen:check` 통과.
- 원 PR 18건의 최신 source head에서 GitHub required `Build & Test`가 모두 통과한 것을 재확인했다.

## 시각 검증 판정과 위험

- 누적 범위는 agent CLI, 계약 테스트, gym·문서·Node wrapper이다. `src/renderer`, pagination, typeset,
  Canvas 출력 경로를 수정하지 않아 별도 HWP 2020 PDF fidelity sweep은 선택하지 않았다. 첨부된 이미지
  증적은 gym·문서 설명용이며 renderer fidelity 주장이 아니다.
- 각 원 PR의 상태, 최신 head, mergeable, required check는 merge 직전에 다시 확인한다. 원격 push, PR 생성,
  merge, close, comment는 작업지시자 승인 전 수행하지 않는다.

## 최종 권고

**메인터너 보정 후 통합 수용 후보.** 최신 source head의 GitHub required check와 mergeability를 merge 직전에
재확인하고 작업지시자 승인을 받은 뒤에만 통합 PR 생성 및 원 PR 후속 처리를 진행한다.
